# บันทึกการเปลี่ยนแปลง (CHANGELOG)

ทุกครั้งที่แก้โปรเจกต์ **ต้องเพิ่มรายการที่ด้านบนสุด** ของไฟล์นี้ (ใหม่สุดอยู่บน) — กฎเต็มดูที่ [CLAUDE.md](CLAUDE.md#กฎการทำงานของทีม-บังคับ)
เป้าหมาย: ใครก็ตาม (คนหรือ agent) เปิดไฟล์นี้แล้วรู้ทันทีว่า "ล่าสุดทำอะไรไป เพิ่มอะไร แก้อะไร ค้างอะไร"

## รูปแบบ (copy ไปใช้)

```markdown
## YYYY-MM-DD — หัวข้อสั้น ๆ (ชื่อคนทำ)
**เพิ่ม**
- ...
**แก้ไข / เปลี่ยน**
- ...
**แก้บั๊ก**
- ...
**ไฟล์หลักที่แตะ:** `path/a.ts`, `path/b.ts`
**ทดสอบ:** npm test (xx ผ่าน) · typecheck ผ่าน · ทดสอบมือ: ...
**ค้าง / ข้อควรรู้:** ... (ถ้าไม่มีให้เขียน "-")
```

---

## 2026-09-25 — ต่อสู้เฟส 3: ท่าทางมอน + ท่าโดนตี/ร่ายเวทของฮีโร่ (Nong + Claude)
**เพิ่ม**
- `pixel-art/monsters/build.py`: `anim_strip()` สร้างแถบ 4 เฟรม (ยืน · หายใจ · โจมตี · โดนตี) ให้มอน **ทุกตัว 32 ตัว** จากภาพต้นฉบับด้วยการเลื่อนแถวพิกเซล 1–2 px
  (puppet — ไม่ resample ภาพยังคม) → `assets/monsters/<sprite>_anim.png` + ภาพตรวจ `pixel-art/monsters/anim.png`
- ในไฟต์: มอนหายใจด้วยการสลับเฟรม (เลือดต่ำสลับเร็วขึ้น) · ตอนกระแทกใช้เฟรมโจมตี · โดนตีใช้เฟรมสะดุ้ง · ร่ายเวทใช้เฟรมโจมตี
- ฮีโร่: ท่าโดนตี = สำเนาเอนหลังสร้างตอนรัน (`flinchTexture` ใน battleJuice) · ท่าร่ายเวท = เฟรมง้างดาบค้างไว้ระหว่างวงเวท
**แก้ไข / เปลี่ยน**
- `sprites.ts` โหลด `<sprite>_anim.png` เพิ่ม (ไม่มีไฟล์ = ใช้ภาพนิ่งแบบเดิม) · แถบลำดับเทิร์นใช้เฟรมแรกของแถบ
**ไฟล์หลักที่แตะ:** `pixel-art/monsters/build.py` (+ anim.png), `apps/game/public/assets/monsters/*_anim.png` (32), `apps/game/src/game/sprites.ts`,
`apps/game/src/scenes/{BattleScene,battleJuice,battleOverlay}.ts`, docs
**ทดสอบ:** npm test ผ่าน · typecheck ผ่าน · ตรวจแถบแอนิเมชันทุกตัวด้วยตา (ตัวเล็ก + บอส) · ในไฟต์: เฟรมสลับ 0/1 ตอนยืน 2 ตอนตี 3 ตอนโดน ·
ฮีโร่สลับเป็นท่าโดนตี/ท่าฟัน · ไม่มี error/warning
**ค้าง / ข้อควรรู้:** ท่าเป็นแบบ puppet ไม่ใช่วาดมือทีละท่า — บอสควรได้ท่าเฉพาะตัววาดมือภายหลัง · เฟส 4 (การตื่นรู้ + Link Attack) ต้องแก้เอนจินให้รับ inputs

## 2026-09-25 — ต่อสู้เฟส 2: เวทีถนนจริง Mode-7 + ฟ้าตามมิติ + เวลาจริง + มิติแตก (Nong + Claude)
**เพิ่ม**
- `scenes/battleStage.ts` (ใหม่): **พื้นสนามคือถนนจริงรอบตัวผู้เล่น** — ใช้ `tileAt` + ตัววาดกระเบื้อง `drawTile` เดิม แล้วฉายแบบ SNES Mode-7 (1/3 ความละเอียด) ·
  ฟ้าตามโลก (สนาม/ชั้นพิกเซล glitch/ชั้นตำนานทอง/บอสโลก) · เส้นตึกขอบฟ้า · **อาคารของประตูที่เข้ามาตั้งอยู่ที่ขอบฟ้า** · โทนกลางวัน/เย็น/กลางคืนตามเวลาจริง ·
  `riftBreak()` เอฟเฟกต์มิติแตกตอนบอสเปลี่ยนเฟส (Post-FX สีเพี้ยน + pixelate + แถบ glitch / เปลวกนกทอง + vignette)
- `scenes/battleOverlay.ts` (ใหม่): แถบลำดับเทิร์น (จาก `combat.queue`) + ไอคอนสถานะใต้หลอด
- `pixel-art/status-icons/build.py` (ใหม่) → `assets/ui/status.png`: ไอคอน 9 แบบ (มึน แข็ง พิษ ไฟ เลือด ช้า ยั่วยุ ถูกรัด โล่)
**แก้ไข / เปลี่ยน**
- จัดตำแหน่งแบบกึ่งสามมิติ: แถวหลังสูง/เล็กลง 10% แถวหน้าใกล้ · ลำดับการวาดตามความลึก · ตัวในแถวเดียวกันเยื้องกัน (ชื่อไม่ทับ)
- ฉากหลังเดิม (ฟ้าไล่สี + พื้นเขียวเรียบ) ถูกแทนที่ทั้งหมด
**ไฟล์หลักที่แตะ:** `apps/game/src/scenes/{BattleScene,battleStage(ใหม่),battleOverlay(ใหม่)}.ts`, `pixel-art/status-icons/*` (ใหม่), `apps/game/public/assets/ui/status.png`, docs
**ทดสอบ:** npm test ผ่าน · typecheck ผ่าน · เบราว์เซอร์: ไฟต์ตอนกลางคืนจริง (ดาว ตึกเปิดไฟ ถนนจริง) · จำลองเวลา 11:00 (ฟ้าใส เมฆ) ·
ประตูห้าง (ชั้นพิกเซล: ฟ้าม่วง + รอยแยก + ห้างที่ขอบฟ้า) → มิติแตกแล้ว post-FX เคลียร์หมด (list = 0) · ประตูวัด (ชั้นตำนาน: ฟ้าทอง ลำแสง วัดที่ขอบฟ้า เปลวกนก) ·
ไอคอนติดไฟขึ้นใต้หลอดฮีโร่ · แถบเทิร์นไม่ทับหัวข้อ 3 บรรทัด · ไม่มี error
**ค้าง / ข้อควรรู้:** พื้นไกล ๆ ที่ข้อมูลแผนที่ยังโหลดไม่ถึงเป็นหมอกสีฟ้า · ปาร์ตี้ใช้ถนนของแต่ละเครื่อง (ต่างคนต่างเห็นถนนตัวเอง) · เฟส 3 (ภาพเคลื่อนไหวมอน/ฮีโร่) ถัดไป

## 2026-09-25 — ต่อสู้เฟส 1: ความมันของการตี (Nong + Claude)
**เพิ่ม**
- แผนยกเครื่องการต่อสู้ 4 เฟส (todo list) ใน ROADMAP "กำลังทำ" — เฟส 1 เสร็จครบ 8 ข้อ
- `scenes/battleJuice.ts` (ใหม่): ตัวเลขดาเมจเด้ง-ตกสีตามธาตุ + "CRITICAL!" + เลนไม่ซ้อนกัน · มอนแตกเป็นพิกเซล (สีจากตัวมันเอง) · เหรียญเด้ง · ตัวนับ COMBO
- `game/haptics.ts` (ใหม่): มือถือสั่นตอนโดนตี/คริ/ท่าไม้ตาย/ตีปิดฉาก/ล้ม + ปุ่มเปิด/ปิดในตั้งค่า
- BattleScene: hit-stop ทั้งฉาก (ชะลอเวลาฉาก แล้วเลื่อนเทิร์นถัดไปออกตาม) · จอสั่นตามดาเมจ · ซูมเข้าตอนคริ/ท่าไม้ตาย · ง้างก่อนพุ่ง ·
  โดนตียุบ+กระเด็น · ท่ายืนหายใจ + เลือดต่ำหอบ/ตัวแดง · เงาใต้ตัว · ลูกศรชี้ตัวที่กำลังเล่น · แถบ HP ลดตอนโดนจริง + แถบขาวไหลตาม ·
  **ตีปิดฉากสโลว์โม** + แฟลช + ซูม/แพน → ท่าฉลอง "VICTORY!" ก่อนหน้าผล · ฝั่งเราล้มลงแทนการจางหาย
**แก้ไข / เปลี่ยน**
- จอสั่นตอนคริย้ายจาก `battleFx.hitFx` มาอยู่ BattleScene (ตามสัดส่วนดาเมจ) · ฉากหลังตรึงกับจอ (ไม่หลุดตอนกล้องแพน)
**ไฟล์หลักที่แตะ:** `apps/game/src/scenes/{BattleScene,battleJuice(ใหม่),battleFx}.ts`, `apps/game/src/game/haptics.ts` (ใหม่), `apps/game/src/ui/hud.ts`, docs
**ทดสอบ:** npm test ผ่าน · typecheck ผ่าน · เบราว์เซอร์: ไฟต์ 3 ตัวที่แพ้ (ล้มลง, เวลา/กล้องกลับปกติ) · ไฟต์ชนะ: สโลว์โม+แฟลช → "2 COMBO" + "VICTORY!" + สไลม์แตกเป็นพิกเซล + เหรียญ →
หน้าผลขึ้น, timeScale = 1, ซูม 1, กล้องกลับที่เดิม · กด "ข้าม" กลางไฟต์ไม่ค้าง · ไม่มี error ในคอนโซล
**ค้าง / ข้อควรรู้:** เฟส 2 (เวที Mode-7/ฟ้าตามชั้นมิติ/เวลาจริง/มิติแตก/แถบเทิร์น/ไอคอนสถานะ) ถัดไป · hit-stop ใช้ `anims.globalTimeScale` (ทั้งเกม) — คืนค่าเป็น 1 ทุกครั้งที่จบ/ปิดฉาก

## 2026-09-25 — รูปหมุดใหม่: สถานที่จริง + ป้ายชื่อใต้หมุด (Nong + Claude)
**เพิ่ม**
- **วาดหมุดใหม่ทั้งหมด** (`pixel-art/landmarks/build.py`) ให้ดูออกทันทีว่าเป็นที่ไหน: ร้านสะดวกซื้อ "24" · ห้าง · ปั๊ม · สถานี · วัดล้านนา ·
  โบราณสถานเสาหิน · ต้นไม้ใหญ่ (บอสโลก) · แผงตลาด · คลินิกกากบาทเขียว · ศาลาเสาแสง (บ้านคงเดิม)
- ประตูเปิด = รอยแยกเคลื่อนไหวอยู่ในช่องประตูของอาคาร (`gate_<ประเภท>.png` 4 เฟรม) · ประตูปิด = อาคารเดิมประตูถูกผนึก [ระบบ] (`sealed_<ประเภท>.png`)
- **ป้ายใต้หมุด**: ชิปแรงก์สีตามแรงก์ + ชื่อที่แอดมินตั้ง (บริการแสดงเฉพาะชื่อ)
**แก้ไข / เปลี่ยน**
- เลิกรูปรอยแยกล้วนแยกตามแรงก์ (`rift_*`, `sealed_<แรงก์>` ถูกลบ) — แรงก์ย้ายไปอยู่บนป้าย · จุดบริการไม่ลอยขึ้นลง/กะพริบแล้ว (เป็นอาคารตั้งบนพื้น)
- ป้ายร้านสะดวกซื้อไม่ใช้แถบส้ม-เขียว-แดง (คล้ายแบรนด์จริง) · โรงพยาบาลใช้กากบาทเขียว (กากบาทแดงเป็นเครื่องหมายคุ้มครอง)
- STORY §5 + MASTER_SPEC §10: กติกาหมุดใหม่ "หมุด = สถานที่จริง + รอยแยกที่ประตู"
**ไฟล์หลักที่แตะ:** `pixel-art/landmarks/build.py` (+ preview.png, frames.png), `apps/game/public/assets/landmarks/*`, `apps/game/src/game/sprites.ts`,
`apps/game/src/scenes/WorldScene.ts`, `apps/game/src/ui/adminWindow.ts`, `pixel-theme.css`, docs
**ทดสอบ:** npm test ผ่าน · typecheck ผ่าน · ตรวจ preview + ทุกเฟรมแอนิเมชัน (แก้ป้ายร้าน + วาดโบราณสถานใหม่ 2 รอบ) ·
ในเกม: วางหมุดครบทุกประเภทรอบตัว → อ่านออกทุกอัน ป้ายชื่อ/แรงก์ขึ้นใต้หมุด ปั๊มที่เพิ่งล้มบอสแสดงแบบผนึก · หน้าต่างแอดมินแสดงรูปอาคารใหม่
**ค้าง / ข้อควรรู้:** ป้ายชื่อซ้อนกับมอนได้ถ้าอยู่ชิดกัน · หมุดทดสอบ 10 อันยังอยู่ในเซิร์ฟเวอร์เครื่องนี้ (ลบได้จากปุ่ม PIN)

## 2026-09-25 — หมุดแผนที่วางโดยแอดมินเท่านั้น (Nong + Claude)
**เพิ่ม**
- **โหมดแอดมิน**: ตั้งค่า → โหมดแอดมิน → ใส่รหัส → ปุ่ม **PIN** ขวามือ เปิดหน้าต่าง "หมุดแผนที่": เลือกประเภท (ประตูมิติ 7 แบบ / ตลาด โรงพยาบาล ศาสนสถาน)
  พร้อมรูปหมุดและบอก บอส/แรงก์/ร้าน ที่จะได้ · ตั้งชื่อ · **แตะบนแผนที่เพื่อวาง** หรือวางตรงที่ยืน · รายการหมุดทั้งหมด (ระยะทาง, ลบ, วาร์ปในโหมดจำลอง)
- เซิร์ฟเวอร์ (`apps/server/src/pins.ts`): เก็บหมุดใน `apps/server/data/pins.json` (ไม่อยู่ใน git) · ส่งหมุดทั้งหมดตอน hello และทุกครั้งที่เปลี่ยน ·
  รหัสแอดมินจาก env `ADMIN_KEY` หรือสุ่มครั้งแรกเก็บใน `data/admin-key.txt` (พิมพ์ตอนเปิดเซิร์ฟเวอร์) · ตรวจรหัสทุกคำสั่ง, ผิด 5 ครั้งล็อกการเชื่อมต่อ
- shared `net/pins.ts` (+เทสต์ 4 ข้อ): `PIN_KINDS`, `sanitizePin` (ตรวจประเภท/พิกัด/ชื่อ ≤30), `PIN_MAX` 2,000 · โปรโตคอล `admin:login`, `pin:add`, `pin:del`, `pins`, `admin`
- เครื่องผู้เล่นจำหมุดล่าสุด (`pw.pins`) ใช้ตอนออฟไลน์ · แอดมินล็อกอินเองอัตโนมัติเมื่อเชื่อมต่อใหม่ (จำรหัสในเครื่อง `pw.adminKey`)
**แก้ไข / เปลี่ยน**
- **เลิกหมุดอัตโนมัติจากข้อมูลแผนที่ทั้งหมด** (`AUTO_POI_PINS = false` ใน `game/world.ts` — โค้ดแปลง POI ยังอยู่ เปิดกลับได้) · มอนสเตอร์ทั่วไปยังเกิดตามเดิม · บ้านของผู้เล่นไม่เปลี่ยน
**แก้บั๊ก**
- ชื่อหมุดที่พิมพ์ค้างไปติดกับประเภทถัดไป → ล้างชื่อเมื่อเปลี่ยนประเภท/วางเสร็จ (เจอระหว่างทดสอบ)
**ไฟล์หลักที่แตะ:** `packages/shared/src/net/{pins,pins.test,protocol}.ts`, `apps/server/src/{pins,index}.ts`, `apps/game/src/game/{admin(ใหม่),world,net,bus}.ts`,
`apps/game/src/ui/{adminWindow(ใหม่),hud}.ts`, `scenes/WorldScene.ts`, `main.ts`, `pixel-theme.css`, `.gitignore`, docs, README
**ทดสอบ:** npm test (115 ผ่าน) · typecheck ผ่าน · ในเบราว์เซอร์ + presence server จริง: หมุดอัตโนมัติหายหมด (มอนยังอยู่) · รหัสผิดขึ้นเตือน รหัสถูกเข้าโหมดแอดมิน ปุ่ม PIN โผล่ ·
วางห้างตรงที่ยืน → การ์ดประตู + ร้านขึ้นทันที · แตะแผนที่วางโรงพยาบาล → แถบ "แตะบนแผนที่เพื่อวาง" แล้วหมุดอยู่ตรงจุดที่แตะ · ลบได้ · รีโหลดแล้วล็อกอินเอง ·
ผู้เล่นคนที่ 2 (ไม่ใช่แอดมิน) ได้หมุดใหม่สดและวาดบนแผนที่ · ส่ง `pin:add`/`pin:del` ตรงเข้าเซิร์ฟเวอร์ด้วยรหัสผิด → ถูกปฏิเสธ, ผิดครบ 5 ครั้งถูกล็อก · ไฟล์ `pins.json` ถูกต้อง
**ค้าง / ข้อควรรู้:** หมุดอยู่ในเครื่องที่รันเซิร์ฟเวอร์ — แต่ละคนในทีมที่รันเซิร์ฟเวอร์เองจะเริ่มจากไม่มีหมุด · ยังแก้ไขหมุดไม่ได้ (ลบแล้ววางใหม่) ·
ดันเจี้ยนแบบ instance (หน้าปาร์ตี้) ยังเปิดได้ทุกที่ ไม่ได้ผูกกับหมุด · พื้นที่ที่แอดมินยังไม่วางหมุดจะไม่มีประตู/ร้าน — ดู ROADMAP "รอตัดสินใจ" ·
ก่อนเปิดสาธารณะต้องผูกแอดมินกับบัญชีจริง + ย้ายหมุดไปฐานข้อมูล

## 2026-09-25 — ทุกไฟต์ใหม่เริ่มด้วย HP/MP เต็ม (Nong + Claude)
**แก้ไข / เปลี่ยน**
- **สู้มอนรอบใหม่ = HP/MP เต็มเสมอ**: `playerSetup()` ไม่ส่ง hp/mp เข้าเอนจินแล้ว (เอนจินเริ่มที่ค่าสูงสุด) — ใช้กับไฟต์เดี่ยว บอส บททดสอบ ปาร์ตี้ และดันเจี้ยน
- หลังจบไฟต์ (ชนะ/แพ้/หนี) เซฟกลับเป็น HP/MP เต็ม — ตายไม่เหลือ 30% แล้ว (โทษตายยังเป็น ชุดพัง + เสีย 20% Gold) · ยาที่ดื่มในไฟต์ยังหายจริง
- ดันเจี้ยน/ประตูมิติหลายเวฟ: HP/MP ยังต่อเนื่องระหว่างเวฟในรอบเดียวกัน (ไม่เปลี่ยน)
- โรงพยาบาล/เขตศักดิ์สิทธิ์: ถ้า HP/MP เต็มปุ่มจะขึ้น "HP/MP เต็มอยู่แล้ว" (ไม่เก็บเงิน/ไม่เสียคูลดาวน์ฟรี)
- `applyBattleOutcome()` ไม่รับ hp/mp แล้ว
**ไฟล์หลักที่แตะ:** `apps/game/src/game/{party,rules}.ts`, `apps/game/src/scenes/BattleScene.ts`, `apps/game/src/ui/hud.ts`, `packages/shared/src/combat/combat.test.ts`, docs
**ทดสอบ:** npm test (111 ผ่าน — เพิ่มเทสต์ "fresh start") · typecheck ผ่าน · ในเบราว์เซอร์: ตั้งเซฟ HP 20/MP 5 → ไฟต์เริ่มเต็ม ชนะแล้วเซฟเต็ม (รวมตอนเลเวลอัป) ·
ตั้ง HP 10 สู้บอสจนตาย → ไฟต์เริ่มเต็ม 219, หลังตายเซฟเต็มตามค่าสูงสุดใหม่ (ชุดพัง), เสีย Gold 20%
**ค้าง / ข้อควรรู้:** โรงพยาบาล เขตศักดิ์สิทธิ์ เตียงในบ้าน ฟื้นฟูตามเวลา และยานอกไฟต์ ตอนนี้ไม่มีประโยชน์แล้ว — รอตัดสินใจบทบาทใหม่ (ROADMAP "รอตัดสินใจ") ·
คำเตือน "HP ต่ำ!" บนการ์ดมอนจะไม่ขึ้นอีก (HP นอกไฟต์เต็มเสมอ)

## 2026-09-25 — ระบบแชท + กรอบคำพูดเหนือหัวตัวละคร (Nong + Claude)
**เพิ่ม**
- **แชท 2 ช่อง**: "ใกล้ ๆ" (ถึงคนที่เห็นตัวเราบนแผนที่อยู่แล้ว + ปาร์ตี้) และ "ปาร์ตี้" — หน้าต่างแชทมุมซ้ายบนใต้แถบสถานะ: แถบย่อบอกข้อความล่าสุด + จำนวนที่ยังไม่อ่าน,
  เปิดแล้วมีประวัติ 40 ข้อความ แท็บเลือกช่อง ช่องพิมพ์ (Enter ส่ง, Esc ปิด) · ข้อความปาร์ตี้มีป้ายและสีเขียว + เสียงแจ้ง
- **กรอบคำพูด pixel เหนือหัวตัวละคร** ทั้งตัวเราและผู้เล่นอื่น (`scenes/chatBubbles.ts`) ตัดบรรทัดภาษาไทยได้ สูงสุด ~3 บรรทัด อยู่ 3–8 วิ
- กติกาแชทใน shared (`net/chat.ts` + เทสต์ 4 ข้อ): ยาวสุด 60 ตัวอักษร · 1.5 วิ/ข้อความ · ตัดอักขระล่องหน · ปิดบังลิงก์ เบอร์โทร @ไอดี คำหยาบ
- เซิร์ฟเวอร์: `chat` ในโปรโตคอล · ตรวจซ้ำ + จำกัดความถี่ + เลือกผู้รับเอง (ไม่ส่งข้อความกลับหาผู้ส่ง)
- ปิดข้อความรายคน: แตะชื่อในแชท (เก็บในเครื่อง)
- **ย่อช่องแชท**: ปุ่ม "–" ย่อเหลือไอคอนเล็ก (ยังมีตัวเลขข้อความใหม่) · แตะไอคอนเปิดแชท ปิดแล้วกลับเป็นไอคอน · ปุ่ม "□" ตอนเปิดคืนแบบเต็ม · จำในเครื่อง (`pw.chatMini`)
**แก้ไข / เปลี่ยน**
- พิมพ์ในช่องแชทไม่ทำให้ตัวละครเดิน (กันปุ่ม WASD ไม่ให้ถึงเกม)
**แก้บั๊ก**
- ข้อความในหน้าต่างแชทซ้อนทับกันเป็นบรรทัดเดียว: คลาสของบรรทัดใช้ชื่อช่อง `near` ซึ่งชนกับคลาส `.near` ของแผงการ์ดด้านล่างจอ
  (position: absolute) → เปลี่ยนเป็น `ch-near` / `ch-party`
- หน้าต่างแชทปิดไม่ลง / ตัวเลข "0" โผล่: คลาส `.hidden` ของเกมใช้ได้เฉพาะบางองค์ประกอบ → เพิ่ม `.chat-box .hidden`
**ไฟล์หลักที่แตะ:** `packages/shared/src/net/{chat,chat.test,protocol}.ts`, `apps/server/src/index.ts`, `apps/game/src/game/{chat,net,bus}.ts` (chat ใหม่),
`apps/game/src/ui/chat.ts` (ใหม่), `apps/game/src/scenes/{chatBubbles(ใหม่),WorldScene,remotePlayers}.ts`, `apps/game/src/ui/hud.ts`, `pixel-theme.css`, docs
**ทดสอบ:** npm test (110 ผ่าน) · typecheck ผ่าน · build ผ่าน · ในเบราว์เซอร์ + presence server จริง 2 ผู้เล่น (`?player=2`):
ส่งข้อความ → อีกเครื่องได้รับ ขึ้นในแชท + กรอบคำพูดเหนือตัว, เบอร์โทรกลายเป็น `***`, ส่งซ้ำเร็วเกินขึ้นเตือน, ปิดข้อความรายคนแล้วไม่ได้รับ,
แชทปาร์ตี้ได้รับพร้อมป้าย "ปาร์ตี้", กรอบคำพูดอยู่เหนือหัวพอดีและตัดคำไทย 3 บรรทัด, พิมพ์ตัว d ในช่องแชทแล้วตัวละครไม่ขยับ
**ค้าง / ข้อควรรู้:** ยังไม่มีรายงาน/บล็อกฝั่งเซิร์ฟเวอร์และบัญชีผู้ใช้ — ห้ามเปิดแชทกับคนแปลกหน้าสาธารณะก่อนมี (MASTER_SPEC §12B) ·
รายการคำหยาบเป็นชุดเริ่มต้นเล็ก ๆ ใน `net/chat.ts` · ประวัติแชทไม่ถูกเก็บ (รีโหลดแล้วหาย) · ตอนต่อสู้ (ฉากต่อสู้) ยังไม่เห็นกรอบคำพูด

## 2026-09-25 — รวม EXP ×10 และความทนทานลดเมื่อตายเข้า main (Nong + Claude)
**แก้ไข / เปลี่ยน**
- รวม `feature/exp-rate` และ `feature/durability-on-death` (อันหลังมีอันแรกอยู่แล้ว รวม 2 commit) เข้า `main` แบบ fast-forward ตามที่เจ้าของโปรเจกต์สั่ง
  ไม่เปิด PR · ไม่มี conflict (ทั้งสอง branch ต่อจาก main ล่าสุด)
- ผลต่อเกม: EXP จากมอน ×10 (`EXP_RATE` ใน `rules/progression.ts` — ลดกลับเมื่อมีเนื้อหาจริง) · อุปกรณ์ไม่ลดความทนทานตอนชนะ/หนี พังทั้งชุดตอนตายเหมือนเดิม
**ไฟล์หลักที่แตะ:** `CHANGELOG.md` (รายละเอียดของแต่ละงานอยู่ในรายการ 2026-09-24 ด้านล่าง)
**ทดสอบ:** บน main หลังรวม: npm test (106 ผ่าน) · typecheck ผ่าน · build ผ่าน
**ค้าง / ข้อควรรู้:** รางวัลเควสรายวันคิดจากหลอด EXP (12% ต่อข้อ) ไม่ได้คูณ ×10 — เมื่อ EXP จากมอน ×10 เควสจึงให้ EXP สัดส่วนน้อยลง ·
ลบ branch `feature/exp-rate` / `feature/durability-on-death` บน GitHub ได้เมื่อไม่ใช้แล้ว

## 2026-09-24 — ปรับผังสกิล Novice เหลือ 4 สกิล (Codex)
**เพิ่ม**
- บัฟต่อสู้แบบค่าตายตัว (`flat`) สำหรับ **Focus**: ATK +1 ชั่วคราว — เอนจินคำนวณร่วมกับบัฟเปอร์เซ็นต์อย่าง deterministic และฉากต่อสู้แสดง `ATK +1` ชัดเจน
- การโจมตีธรรมดาแสดงคำว่า **“โจมตี”** เหนือตัวละครแล้ว; ก่อนหน้านี้มีใน event log แต่ไม่มีชื่อเด้งบนจอ จึงดูเหมือนสกิลออกถี่เกินจริง
- เทสต์ยืนยันว่า flat buff เพิ่ม ATK ได้ตรงค่า และตรวจว่า skill pool ของ Novice มีเพียง 4 สกิล
**แก้ไข / เปลี่ยน**
- **Novice มี 4 สกิลเท่านั้น**: Rush (180% ATK) · Throw Stone (110% ATK, โจมตีแถวหลัง) · Focus (ATK +1 ชั่วคราว) · Counter Tackle (ถูกตีโดน 15% สวน 60% ATK) · โจมตีธรรมดาเป็น fallback ของเอนจิน ไม่ใช่สกิลใน deck
- ตัด skill pool / preset เดิม 14 สกิลและ 5 preset ออก เหลือชุดเริ่มต้น 4 สกิล; ทุกอาชีพขั้นถัดไปยังหยิบ 4 สกิล Novice ร่วมกับสกิลอาชีพได้
- โหลดเซฟเก่าที่มีสกิล Novice ที่ถูกถอดแล้วจะรีเซ็ต deck เป็นค่าเริ่มต้นที่ถูกต้อง แทนการเหลือ deck ไม่ครบ
- ปรับสมดุล Counter Tackle และ Throw Stone หลังจำลอง: Lv.10 Novice ผ่าน Goblin King ~31% · Ghost Alley solo Lv.5 ~77% · Lv.20 Knight ผ่าน Octane ~28%
**ไฟล์หลักที่แตะ:** `packages/shared/src/data/{skills,classes}.ts`, `packages/shared/src/combat/{types,engine}.ts`, `apps/game/src/{scenes/BattleScene,state/store}.ts`, `docs/{MASTER_SPEC,COMBAT_SPEC,ROADMAP}.md`
**ทดสอบ:** npm test (104 ผ่าน) · typecheck ผ่าน · จำลอง 1,000 ไฟต์ / 60,000 เทิร์น: Novice ตีธรรมดา 58.9% · Throw Stone 17.6% · Rush 13.4% · Focus 10.1% (ก่อนคิด Counter Tackle)
**ค้าง / ข้อควรรู้:** Rush, Throw Stone และ Counter Tackle ใช้ id/ไอคอนเดิม (`power_smash`, `stone_throw`, `counter_jab`) ชั่วคราวเพื่อให้ภาพและเซฟเข้ากันได้; ถ้าต้องการภาพเฉพาะของชื่อใหม่ ให้เพิ่ม source ใน `pixel-art/skill-icons/build.py` แล้วสร้าง PNG ใหม่

## 2026-09-24 — ความทนทานของอุปกรณ์ลดเมื่อตายเท่านั้น (Nong + Claude)
**แก้ไข / เปลี่ยน**
- **เลิกลดความทนทาน 1 แต้มทุกการต่อสู้** — ชนะหรือหนี อุปกรณ์ไม่ลดเลย · **ตาย = ทุกชิ้นพัง (0)** เหมือนเดิม
- ย้ายกติกาไปไว้ฝั่ง shared: `durabilityAfterBattle(durability, result)` ใน `rules/economy.ts` (เดิมเขียนตรง ๆ ใน `apps/game/src/game/rules.ts`)
- ตีบอสโลกแล้วหมดแรง ระบบนับเป็น "หนี" อยู่แล้ว → ของไม่พัง
**ไฟล์หลักที่แตะ:** `packages/shared/src/rules/economy.ts`, `packages/shared/src/rules/rules.test.ts`, `apps/game/src/game/rules.ts`, `docs/MASTER_SPEC.md` §9
**ทดสอบ:** npm test (106 ผ่าน — เพิ่มเทสต์ ชนะ/หนีไม่ลด, ตายเหลือ 0, ชนะ 100 ครั้งติดยังเต็ม) · typecheck ผ่าน ·
ในเกม: ซ่อมเป็น 40 แล้วชนะ 3 ครั้ง → ยัง 40 ทุกชิ้น · ตั้ง HP 1 ไม่มียาแล้วแพ้ → ทุกชิ้นเหลือ 0 (คืนค่าตัวทดสอบแล้ว)
**ค้าง / ข้อควรรู้:** ของพังอย่างเดียวคือตอนตาย รายจ่ายค่าซ่อม/หินลับมีดจะน้อยลงมาก — ร้านตีเหล็กกับกล่องเครื่องมือช่างอาจขายไม่ออก
ถ้าเห็นว่าตายแล้วพังหมดแรงไป อาจเปลี่ยนเป็นลดทีละ % แทนได้ (แก้ที่ `durabilityAfterBattle` ที่เดียว)

## 2026-09-24 — เลเวลขึ้นไวขึ้น: EXP จากมอน ×10 (Nong + Claude)
**เพิ่ม**
- `EXP_RATE` ใน `rules/progression.ts` (ตอนนี้ = **10**) คูณกับ EXP ที่ `rollLoot()` จ่าย — ปรับเลขเดียวคุมความเร็วเลเวลทั้งเกม
- เทสต์ใหม่: EXP ที่ได้ = `exp ของมอน × EXP_RATE` · เลเวลแรกต้องใช้ไม่เกิน 3 ตัว · Gold/ของดรอปไม่เปลี่ยน
**แก้ไข / เปลี่ยน**
- ขึ้นเลเวลเร็วขึ้น ~10 เท่า: จากเดิมประมาณ 20–30 ตัว/เลเวล เหลือ ~2–3 ตัว/เลเวล (เส้นโค้ง `100 × L^1.5` เหมือนเดิม)
- รางวัลเควสรายวันขยับตามเอง เพราะคิดเป็น 12% ของหลอด EXP ระดับนั้นอยู่แล้ว
**ไฟล์หลักที่แตะ:** `packages/shared/src/rules/{progression,loot}.ts`, `packages/shared/src/rules/rules.test.ts`, `docs/MASTER_SPEC.md`
**ทดสอบ:** npm test (105 ผ่าน) · typecheck ผ่าน · ในเกม (ตัวละคร Lv.5): สู้ 3 ครั้งได้ EXP 275 / 374 / 495 → ขึ้น Lv.6
(หลอดของ Lv.5 = 1,118) เฉลี่ยประมาณ 3 ตัวต่อเลเวล
**ค้าง / ข้อควรรู้:** ×10 เป็นค่าสำหรับ**ช่วงทดสอบ** ต้องทบทวนก่อนเปิดให้คนนอกเล่น · เลเวลขึ้นไวแปลว่าแต้มสเตตัสมาไวด้วย (5 แต้ม/เลเวล)
สมดุลการต่อสู้ช่วงกลางเกมอาจง่ายเกินจริง · มอนสเตอร์ช่วง Lv.15–35 ยังมีน้อย ผู้เล่นจะทะลุช่วงนี้เร็วมาก

## 2026-09-24 — รวมงานชุด/ท่ายืน/ท่าฟัน/skill tree เข้า main (Nong + Claude)
**แก้ไข / เปลี่ยน**
- รวม `codex/novice-skill-tree` เข้า `main` แบบ fast-forward (main ไม่มีงานใหม่ระหว่างนั้น ไม่มี conflict) — เจ้าของโปรเจกต์สั่งรวมตรง ไม่เปิด PR
  ได้ทั้งหมด 4 commit: ชุดวาดมือ 2 ชุด · ท่ายืน · Simplify Novice skill tree (อีก session) · ท่าฟันดาบ
- branch ที่ push ขึ้น GitHub แล้ว: `feature/outfit-pack` (ชุด + ท่ายืน) และ `codex/novice-skill-tree` (ครบทุกงาน) — ลบได้เมื่อไม่ใช้แล้ว
**ทดสอบ:** บน main หลังรวม: npm test (104 ผ่าน) · typecheck ผ่าน · build ผ่าน
**ค้าง / ข้อควรรู้:** repo เป็น public และมีภาพ placeholder จากเกม SoC (body/ผม รวมเฟรมท่าฟัน) อยู่บน origin —
HANDOFF ของฝั่ง 48bit ห้ามไว้ ถ้าจะให้ตรงกฎต้องเปลี่ยน repo เป็น private จนกว่าจะวาดตัวหุ่นของเราเอง

## 2026-09-24 — ท่าฟันดาบในฉากต่อสู้ + อัปเดต avatar pack รอบใหม่ (Nong + Claude)
> commit นี้**รวมงานของ session ก่อนหน้าที่ยังไม่ได้ commit ทิ้งไว้ในเครื่อง**มาด้วย (แยกไม่ได้เพราะท่าฟันเขียนต่อจากมันโดยตรง):
> `lungeAtTarget()` พุ่งเข้าหาเป้าหมายในฉากต่อสู้ · `meleeSwing()` เอฟเฟกต์ฟันเส้นทองใน `battleFx.ts` ·
> pixel art `pixel-art/novice-battle-attack/` + `assets/characters/novice_battle_slash.png` · บันทึกสกิล `learned/006-chibi-turning-melee-combo.md`
> งานส่วนนั้นผมไม่ได้เขียนเองและยังไม่ได้ทดสอบแยก — ถ้าจะแก้ต่อให้ดูที่ไฟล์พวกนี้ก่อน

**เพิ่ม**
- **ท่าฟันดาบ 3 เฟรม** (ง้าง / ฟัน / ตามแรง) จาก pack ของทีม 48bit — เล่นตอน**พุ่งเข้าหาเป้าหมาย**ด้วยสกิลระยะประชิด
  ใน `lungeAtTarget()`: ง้างดาบตอนพุ่งเข้า → เฟรมฟันตรงจังหวะดาเมจลง (`hit_frame = 1`) → ตามแรงตอนถอยกลับ → กลับท่าเดิม
- ชั้นภาพใหม่ในการประกอบตัวละคร: `fx` (เส้นฟัน วาดหลังตัว) · `weapon` (ดาบของ pack ใช้แทนอาวุธที่โค้ดวาดระหว่างฟัน) ·
  `hand` (มือกำดาบ ย้อมสีผิวแบบเดียวกับตัว) — ลำดับ fx → body → outfit → hair → weapon → hand ตาม HANDOFF
- `animFrameCount()` / `avatarAction()` ใน `game/avatar.ts` อ่านจำนวนเฟรมและเวลาของท่าจาก manifest
**แก้ไข / เปลี่ยน**
- ก๊อป avatar pack รอบใหม่ทับทั้งโฟลเดอร์ (body เพิ่ม `slash_0..2`, ชุดทั้ง 2 มีเฟรมฟันครบ, เพิ่ม `hand/`, `weapon/W01_short_sword/`,
  `fx/slash/`, `preview/`) — manifest มี `outfits` / `actions` / `anims.slash` ของทุกทรงผม
- `heroCanvas(doll, facing, frame, pose)` เปลี่ยนพารามิเตอร์ที่ 4 จาก `idle: boolean` เป็น `pose: 'walk' | 'idle' | 'slash'`
- ตัวละครที่ใช้ชุดจาก pack เลิกใช้เอฟเฟกต์ฟันแบบเดิม (`meleeSwing`) เพราะภาพท่าฟันมีเส้นฟันมาในตัวแล้ว — ตัวละครแบบเก่ายังใช้เหมือนเดิม
- แผ่นรีวิว `tools/artpreview/avatar.mts` เพิ่มคอลัมน์ท่าฟัน 3 เฟรม
**ไฟล์หลักที่แตะ:** `apps/game/public/assets/avatar/**` (pack ใหม่ทั้งชุด), `apps/game/src/game/{avatar,gear,art}.ts`,
`apps/game/src/scenes/{BattleScene,WorldScene,remotePlayers}.ts`, `tools/artpreview/avatar.mts`, `docs/MASTER_SPEC.md`
**ทดสอบ:** npm test (104 ผ่าน) · typecheck ผ่าน · build ผ่าน · `npx tsx tools/artpreview/avatar.mts` ท่าฟันขึ้นครบทั้ง 11 ลุค ·
ในเกม: เข้าสู้แล้ว texture ของฮีโร่ไล่ `side_0 → slash_0 → slash_1 → slash_2 → side_0` (พุ่งเข้า 4 ครั้งในหนึ่งการต่อสู้) ·
หยุดเกมที่เฟรมฟันแล้วดูภาพ: ตัวละครถึงตัวมอน ดาบกางพร้อมเส้นฟัน · กลับมาบนแผนที่ยังเล่นท่ายืนปกติ
**ค้าง / ข้อควรรู้:** ระหว่างท่าฟันใช้**ดาบของ pack เสมอ** ไม่ว่าผู้เล่นถืออาวุธอะไร (ไม้เท้า/หอกก็เห็นเป็นดาบ) — รอ art ของอาวุธอื่นจากฝั่ง 48bit ·
ท่าฟันมีเฉพาะฝั่งผู้เล่น มอนสเตอร์ยังใช้เอฟเฟกต์เดิม · เวลาใน `actions.slash.frame_ms` ([260, 90, 300]) ยังไม่ได้ใช้ตรง ๆ
เพราะผูกเฟรมกับจังหวะพุ่ง/ฟัน/ถอยของ `lungeAtTarget` เพื่อให้ตรงกับดาเมจและความเร็วเกม (2×/4×)

## 2026-09-24 — ชุดตัวละครวาดมือ 2 ชุด แทนชุดเดิม 4 แบบ (Nong + Claude)
**เพิ่ม**
- **ชุดจาก avatar pack ของทีม 48bit**: `A01_starter_leather` (ชุดหนัง) และ `A02_light_armor` (ชุดเกราะเบา) ชุดละ 21 เฟรม
  ลงที่ `assets/avatar/outfit/<id>/` + อัปเดต body เป็น 21 เฟรม (เพิ่ม wait_4, wait_5) · ภาพสีจริง RGBA **ห้าม recolour**
- `composeAvatar()` วาดชั้นชุดจาก PNG: ใส่ชุดจาก pack แล้ว**ข้าม** `paintLegwear`/`paintBoots`/`paintChest` ไม่วาดทับกัน
  (ชุดมีกางเกง/รองเท้า/ปลอกแขนมาในไฟล์เดียว) · ชื่อไฟล์ชุดอ้างจาก `frame.body` เสมอ · id ชุดรวมใน cache key
- `PACK_OUTFITS` ใน `game/gear.ts` แปลง `EquipmentDef.sprite` → id ของ pack (ฝั่ง `packages/shared` ไม่รู้จักไฟล์ภาพฝั่ง client)
- **ท่ายืน (idle) ใช้งานจริงแล้ว** — เดิมมีภาพใน pack แต่เกมไม่เคยเรียก: ยืนนิ่งเกิน 0.3 วิ → เล่นท่ายืนวน 350 ms/เฟรม
  (6–10 เฟรมแล้วแต่ทรงผม) พอขยับก็ตัดกลับท่าเดินทันที · ใช้ทั้งตัวเราและผู้เล่นคนอื่นบนแผนที่
  (`idleFrameCount()`, `IDLE_DELAY_MS`, `IDLE_MS` ใน `game/avatar.ts` · `heroCanvas(doll, facing, frame, idle)`)
- แผ่นรีวิวภาพ `tools/artpreview/avatar.mts` เพิ่มคอลัมน์ท่ายืน 4 เฟรม (เดิมมีแค่เฟรมเดียว)
- เทสต์ใหม่ 3 ข้อ: ช่องชุดมี 2 ชิ้น, ชุดเริ่มต้นใส่ได้ที่ Lv.1, ชุดเก่าทุกตัวแปลงเป็นชุดเกราะเบา
**แก้ไข / เปลี่ยน**
- **ช่องชุดเหลือ 2 ชิ้น**: ชุดหนังนักเดินทาง (Lv.1, DEF+5 HP+20, 150 G) · ชุดเกราะเบา (Lv.5, DEF+18 HP+60 VIT+3, 450 G)
  เลิกใช้ เสื้อผ้าฝ้ายดิบ / เสื้อม่อฮ่อม / เกราะหวายสาน / เกราะหัวจ่ายน้ำมัน
- **ตัด "สีเสื้อ" ออกจากหน้าสร้างตัวละคร** (ชุดวาดมือย้อมสีไม่ได้) — ฟิลด์ `outfit` ในเซฟยังอยู่เพื่อไม่ให้เซฟเก่าพัง
- เซฟเก่า: ชุดที่ถูกยกเลิก**ยังสวมอยู่ช่องเดิม** ไม่ถูกถอดลงกระเป๋าอีกต่อไป (`migrateLegacyGear` ใส่ของแทนที่กลับช่องเดิมถ้าเป็นช่องเดียวกัน)
  เกราะหวาย/เกราะหัวจ่ายได้ส่วนต่างราคาคืนเป็น Gold (950 / 3,050)
- บอสหุ่นยนต์หัวจ่ายเลิกดรอปเกราะหัวจ่าย → ดรอปกล่องเครื่องมือช่าง 15% แทน · ร้านค้า 3 ร้านปรับ stock ตามชุดใหม่
- ไอคอนกระเป๋า: วาด `leather_garb` (หนังผูกเชือก) กับ `light_armor` (เกราะเหล็กทับหนัง) ใหม่ ลบไอคอนของชุดที่เลิกใช้
**ไฟล์หลักที่แตะ:** `apps/game/public/assets/avatar/{outfit/*,body/*,manifest.json}`, `apps/game/src/game/{gear,avatar,art}.ts`,
`apps/game/src/scenes/{WorldScene,remotePlayers}.ts`, `apps/game/src/ui/creator.ts`, `packages/shared/src/data/{items,shops,monsters}.ts`, `packages/shared/src/rules/starter.ts`,
`packages/shared/src/rules/{starter,shop}.test.ts`, `pixel-art/item-icons/build.py`, `tools/artpreview/*.mts`, `docs/MASTER_SPEC.md`
**ทดสอบ:** npm test (103 ผ่าน — เดิม 100 + เทสต์ใหม่ 3) · typecheck ผ่าน · build ผ่าน · `npx tsx tools/artpreview/avatar.mts` ดูครบ 9 เฟรม × 11 ลุค ชุดตรงตัวหุ่นทุกเฟรม ·
ในเบราว์เซอร์: หน้าสร้างตัวละครไม่มีแถบสีเสื้อแล้ว ใส่ชุดหนังขึ้นถูก · เดินบนแผนที่ปกติ · สลับเป็นชุดเกราะเบา Lv.5 → DEF 31 ไฟล์ชุดโหลดครบ ไม่มี error ใน console ·
ท่ายืน: ยืนเฉย → texture วน `idle_0…idle_5`, กด D เดิน → `side_0…side_3` ทันที, ปล่อยปุ่ม 0.12 วิ → ยังเป็นท่ายืนนิ่ง, เกิน 0.8 วิ → เข้าท่ายืนขยับ ·
เปิด 2 แท็บ (`?player=2`): ผู้เล่นอีกคนบนแผนที่เล่นท่ายืนคนละจังหวะกัน
**ค้าง / ข้อควรรู้:** ชุดยัง **ย้อมสีไม่ได้** (tone map จะตามมาทีหลังจากฝั่ง 48bit) · ตัวหุ่น body ยังเป็น placeholder จากเกม SoC →
คง `USE_AVATAR_PACK` เป็น dev flag และ **ห้าม deploy สาธารณะ** · ต้องการแก้ภาพชุดให้แจ้งฝั่ง 48bit วาดใหม่ ห้ามแก้ PNG ด้วยมือ ·
ยังไม่มีชุดสำหรับเลเวลสูง (หลัง Lv.5 ไม่มีของใหม่ในช่องชุด) ·
ท่ายืนมีเฉพาะท่าหันหน้าเข้าหากล้อง (pack ไม่มีท่ายืนหันข้าง/หันหลัง) — ยืนหันหลังอยู่แล้วหยุดเดินจะหันหน้ากลับมา ·
`wait_4.png` / `wait_5.png` ที่เพิ่งเพิ่มเข้า body ยังไม่มีทรงผมไหนอ้างถึงใน manifest — ต้องถามฝั่ง 48bit ว่าควรใส่ใน idle ของทรงผมไหน ·
หน้าสร้างตัวละคร/หน้าตัวละครยังโชว์ท่าเดินเหมือนเดิม (ตั้งใจ ให้ดูชุดตอนเดิน)

## 2026-09-24 — สรุปส่งต่อทีม (เช้า): รวมเนื้อเรื่อง + หมุดใหม่เข้า main (Nong + Claude)
**แก้ไข / เปลี่ยน**
- `docs/ROADMAP.md` หัวข้อ "📦 ส่งต่อทีม" อัปเดตเป็นรอบ 2026-09-24: ชี้ไป STORY.md, สรุปงานเช้านี้ + เมื่อวาน, วิธีทดสอบเมืองอื่น,
  ข้อควรรู้ (UI 2 ชั้น, ห้าม `sed -i` กับไฟล์ CRLF), งานที่แนะนำทำต่อ
- รวม `feature/story-world` (2 commit) เข้า `main` แบบ fast-forward (main ไม่มีงานใหม่ระหว่างนั้น ไม่มี conflict) — เจ้าของโปรเจกต์สั่ง push ตรงเข้า main
**ไฟล์หลักที่แตะ:** `docs/ROADMAP.md`, `CHANGELOG.md`
**ทดสอบ:** บน main หลังรวม: npm test (100 ผ่าน) · typecheck ผ่าน · build ผ่าน
**ค้าง / ข้อควรรู้:** ดูหัวข้อ "ส่งต่อทีม" และ "กำลังทำ" ใน ROADMAP

## 2026-09-24 — หมุดแผนที่ใหม่ตามเนื้อเรื่อง: ประตู = รอยแยก (Nong + Claude)
**เพิ่ม**
- `pixel-art/landmarks/build.py` เขียนใหม่ทั้งหมด (ผ่าน concept 2 รอบกับเจ้าของโปรเจกต์):
  - **ประตูเป็นรอยแยกล้วน ๆ ไม่มีตึก** — รอยฉีกซิกแซกกลางอากาศ 3 แบบ × 6 แรงก์: ชั้นพิกเซล (มืด ข้อมูลไหล พิกเซลแตกลอยขึ้น มีเส้นกระตุก) ·
    ชั้นตำนาน (แสงทอง ขอบเปลวกนก ประกายลอยขึ้น) · บอสโลก (ใหญ่สุด มีมงกุฎ) — แบบเปิดเป็นแอนิเมชัน 4 เฟรม
  - **ประตูปิด** = รอยร้าวเทาบาง ๆ มีแถบผนึก [ระบบ] 3 แถบ แม่กุญแจสีตามแรงก์
  - จุดบริการ (รพ./ตลาด) = เพชรโฮโลแกรม [ระบบ] ลอยขึ้นลง · ศาสนสถาน = เสาแสงบนวงใบบัว ไม่มีสัญลักษณ์ศาสนา (หายใจเบา ๆ) · บ้าน = บ้านไฟอุ่นมีธง
- `gateLayer()` ใน shared (ชั้นของประตูตามสถานที่) · รอยแยกแต่ละอันเริ่มแอนิเมชันคนละเฟรม ไม่กระพริบพร้อมกันทั้งเมือง
**แก้ไข / เปลี่ยน**
- ลบภาพหมุดแบบตึกเดิมทั้งหมด · เลิกใช้ตัวอักษรแรงก์ลอยบนหมุด (แรงก์ดูจากสีขอบ/ความสูง และป้ายบนการ์ด)
- หมุดประตูสลับภาพเองเมื่อบอสถูกปราบ (ปิด) และเมื่อเกิดใหม่ (เปิด)
**ไฟล์หลักที่แตะ:** `pixel-art/landmarks/build.py`, `apps/game/public/assets/landmarks/*` (28 ไฟล์ใหม่ แทนของเดิม), `apps/game/src/game/sprites.ts`,
`apps/game/src/scenes/WorldScene.ts`, `packages/shared/src/rules/rank.ts`, docs
**ทดสอบ:** npm test (100 ผ่าน) · typecheck ผ่าน · build ผ่าน · ในเบราว์เซอร์ที่เชียงใหม่: รอยแยกทองของวัด 27 · รอยแยกม่วงพิพิธภัณฑ์ 14 · รอยแยกมืดร้านค้า 18 ·
เพชรตลาด 3 · เสาแสงศาสนสถาน 2 · ตั้งบอสวัดเป็นถูกปราบ → หมุดเปลี่ยนเป็นรอยร้าวผนึกทันที (ล้างค่าทดสอบคืนแล้ว)
**ค้าง / ข้อควรรู้:** ถ้าเปลี่ยนเลเวลบอสจนแรงก์เปลี่ยน ภาพรอยแยกเปลี่ยนตามเองโดยไม่ต้องวาดใหม่ (มีครบทุกแรงก์) · ภาพ concept อยู่ในแชต ไม่ได้เก็บใน repo

## 2026-09-24 — เนื้อเรื่อง + หมุดทั่วโลก + เควสรายวัน + UI [ระบบ] (Nong + Claude)
**เพิ่ม**
- **`docs/STORY.md` story bible** (แบบผสม: ประตูพิกเซลแนว Solo Leveling + ชั้นตำนานล้านนา): โลก 2 ชั้น, [ระบบ] และ Walker, แรงก์,
  บทบาทสถานที่ทุกประเภท, กติกาสร้างมอน/บอส/ไอเทม/สกิล/UI ให้เข้าเรื่อง, เส้นเรื่อง 4 บท — ลิงก์ไว้ใน CLAUDE.md
- **หมุดทั่วโลกจากข้อมูล OpenFreeMap** (ศึกษาจากไฟล์ vector tile จริง 8 เมือง: `tools/osm/probe-pois.mts`):
  สถานีรถไฟ/ขนส่ง/ท่าเรือ · พิพิธภัณฑ์/โบราณสถาน/ปราสาท/อนุสาวรีย์ · โรงพยาบาล · ตลาด · ศาสนสถานทุกศาสนา (ป้ายตามศาสนา) —
  ห้างจับ `department_store` ครบทุกประเภท · ลดหมุดซ้ำในเมืองใหญ่
- **บอสประตูใหม่ 2 ตัว** + ภาพ pixel: นายสถานีเงา (สถานี, Lv.22) · ศิลาผู้พิทักษ์โบราณ (พิพิธภัณฑ์, Lv.33) + ของรางวัล 2 ชิ้น
- **บริการ**: โรงพยาบาล (ฟื้นฟูเต็มจ่าย Gold) · ศาสนสถาน = เขตศักดิ์สิทธิ์ "พักใจ" ฟรีทุก 1 ชม. · ตลาดรับซื้อของดรอป +20% ขายข้าวเหนียวหมูปิ้ง/ชาเย็น
- **แรงก์ E–S จากเลเวล** (ป้ายบนแถบบน, บนการ์ดประตู และตัวอักษรสีบนหมุดประตู)
- **เควสรายวันจาก [ระบบ]** เน้นปราบมอน: ปุ่ม "เควส" + ป้ายแจ้งเมื่อรับรางวัลได้ · ผลการต่อสู้บอก "[ระบบ] เควสสำเร็จ" · ครบ 3 ข้อได้แต้มสเตตัส +1
- **UI ชั้น [ระบบ]**: กรอบโฮโลแกรม `sys` / `sys-gold` / `sys-red` (ui-kit) · ทุกแจ้งเตือนพูดในนาม [ระบบ] · หน้าต่างเลเวลอัปกลางจอ ·
  การ์ดประตู · ชนะบอส = "ปิดประตูมิติสำเร็จ!" · เริ่มสู้บอส "[ระบบ] ประตูระดับ X" · **หน้าการตื่นรู้** ก่อนสร้างตัวละคร
- ภาพ pixel ใหม่: หมุด 5 แบบ + รอยแยกหลังหมุดประตู (ม่วง = ชั้นพิกเซล, ทอง = ชั้นตำนาน) · ไอคอน 4 ชิ้น · ไอคอน UI เควส/ประตู
- เทสต์ `rules/story.test.ts` 10 ข้อ (แรงก์, บริการ, เควสรายวัน, สมดุลบอสใหม่ 2 ตัว)
**แก้ไข / เปลี่ยน**
- เวลาเกิดบอสโลก (18:00) และรีเซ็ตเควสใช้เวลาท้องถิ่นของผู้เล่น (เดิมล็อกเวลาไทย)
- เมนูล่างมี 6 ปุ่ม (เพิ่ม "เควส") ย่อขนาดบนจอแคบ
**ไฟล์หลักที่แตะ:** `docs/STORY.md` (ใหม่), `packages/shared/src/rules/{rank,daily}.ts` (ใหม่), `rules/story.test.ts` (ใหม่), `types.ts`, `data/{monsters,items,shops}.ts`,
`rules/{shop,economy}.ts`, `apps/game/src/ui/systemUi.ts` (ใหม่), `ui/hud.ts`, `ui/shopWindow.ts`, `game/{world,rules,sprites}.ts`, `state/store.ts`,
`scenes/{WorldScene,BattleScene}.ts`, `main.ts`, `pixel-theme.css`, `pixel-art/{landmarks,monsters,item-icons,ui-kit}/build.py`, `tools/osm/probe-pois.mts` (ใหม่)
**ทดสอบ:** npm test (100 ผ่าน) · typecheck ผ่าน · build ผ่าน · ในเบราว์เซอร์: เชียงใหม่ (วัด 75, ตลาด 18, พิพิธภัณฑ์/โบราณสถาน 27, ศาสนสถานอื่น 24, รพ. 4
ในรัศมี 1.5 กม.) และวาร์ปไปชิบุยะ (สถานีรถไฟ 12, ศาลเจ้า 20, โบสถ์ 19 → การ์ด "ประตู C · นายสถานีเงา") · ชนะมอน 3 ตัว → เควสนับ + ขึ้นในผลการต่อสู้,
กดรับรางวัล +52 G · พักใจ HP 10→104 · โรงพยาบาลรักษาเต็มจ่าย 26 G · หน้าการตื่นรู้/หน้าเควส/จอมือถือ 375 px
**ค้าง / ข้อควรรู้:** ชั้นตำนานยังเป็นตำนานไทยทุกประเทศ · เจ้าแห่งบั๊ก (แรงก์ S) ยังไม่ทำ · รางวัลเควสคำนวณในเครื่องผู้เล่น ·
**ห้ามใช้ `sed -i` แก้ไฟล์ CRLF บน Windows (Git Bash)** — มันลบ CR ทั้งไฟล์ ทำให้ diff บวม (ใช้ Edit หรือสคริปต์ Python ที่รักษา line ending)

## 2026-09-23 — สรุปส่งต่อทีม: รวมงานทั้งวันเข้า main (Nong + Claude)
**เพิ่ม**
- `docs/ROADMAP.md` หัวข้อ "📦 ส่งต่อทีม": สรุป 7 เรื่องที่ทำวันนี้, วิธีเริ่มงานเครื่องใหม่, ข้อควรรู้ก่อนแก้โค้ด, งานที่แนะนำทำต่อ
**แก้ไข / เปลี่ยน**
- รวม branch `feature/creator-gear` → `feature/monsters-16bit` → `feature/bag-party-ui` → `feature/fx-audio` → `feature/shop`
  เข้า `main` แบบ fast-forward (ไม่มีงานใหม่บน main ระหว่างนั้น จึงไม่มี conflict) — เจ้าของโปรเจกต์สั่ง push ตรงเข้า main แทนการเปิด PR
- ปรับ "ปัญหาที่รู้แล้ว" ใน ROADMAP ให้ตรงสถานะปัจจุบัน
**ไฟล์หลักที่แตะ:** `docs/ROADMAP.md`, `CHANGELOG.md`
**ทดสอบ:** บน main หลังรวม: npm test (90 ผ่าน) · typecheck ผ่าน · build ผ่าน
**ค้าง / ข้อควรรู้:** ดูหัวข้อ "ส่งต่อทีม" และ "กำลังทำ" ใน ROADMAP · branch feature ทั้ง 5 ยังอยู่ในเครื่องนี้ (ไม่ได้ push)

## 2026-09-23 — ระบบร้านค้า + อุปกรณ์ใหม่ 10 ชิ้น + ของดรอปจากมอนสเตอร์ (Nong + Claude)
**เพิ่ม**
- **ร้านค้า** (`ui/shopWindow.ts`, ข้อมูล `packages/shared/src/data/shops.ts`, กติกา `rules/shop.ts`):
  ร้านสะดวกซื้อ/ห้าง/ปั๊มน้ำมันบนแผนที่มีปุ่มร้านในการ์ดแลนด์มาร์ก · แท็บ **ซื้อ** = การ์ดไอเทม (กรอบสีตามระดับ, ป้าย Lv. แดงถ้าเลเวลไม่ถึง, ราคา)
  + กล่องรายละเอียด (ค่าสถานะ, เลเวลที่ต้องการ, ประวัติไอเทม, ซื้อ ×1/×5/×10) · แท็บ **ขาย** = ขายของดรอปทีละชิ้น/ทั้งหมด/ปุ่มเดียวขายหมด,
  ขายยาและอุปกรณ์ในกระเป๋า · ร้านตีเหล็กมีบริการซ่อม · ชั้นวางในบ้านใช้หน้าต่างเดียวกันแต่ไม่รับซื้อ
- **อุปกรณ์ใหม่ 10 ชิ้น** มีระดับ/ราคา/เลเวล/ประวัติ: งอบใบลาน · หอกไม้ไผ่ · พวงมาลัยมะลิไม่รู้โรย · เสื้อม่อฮ่อม · มีดหมอล้านนา · เกราะหวายสาน ·
  ไม้เท้านาคาแม่ปิง · หมวกสำริดกำแพงเก่า · ดาบยามแจ่งเมือง · เครื่องรางช้างศิลา — ชุด/หมวก/อาวุธแสดงบนตัวละคร (`game/gear.ts`)
- **ของดรอป 23 ชนิด** (`MATERIALS`): มอนสเตอร์ทุกตัวดรอปของประจำตัว (เมือกสไลม์ ขนนกพิราบ หางหนู … เศษหินศิลา) + ของรางวัลบอส 5 ชิ้น
- ไอคอน pixel ใหม่ 33 ชิ้นใน `pixel-art/item-icons/build.py` (รวมทั้งชุด 50 ไอคอน)
- ไอเทมเดิมทุกชิ้นได้ `level` + `lore` (ประวัติ) · ยาและเครื่องมือได้ประวัติด้วย
- เทสต์ `rules/shop.test.ts` 13 ข้อ: ของในร้านมีจริง, ร้านไม่ขายขยะ, บ้านไม่รับซื้อ, ทุกไอเทมมีประวัติ, id ไม่ซ้ำ, ระดับสูงแพงกว่า,
  ล็อกเลเวล, ราคาขาย, ซื้อ-ขายคืนไม่ได้กำไร, มอนทุกตัวดรอปขยะและตัวแข็งกว่าดรอปของแพงกว่า
**แก้ไข / เปลี่ยน**
- สวมอุปกรณ์ที่เลเวลไม่ถึงไม่ได้ (ขึ้นข้อความบอกเลเวลที่ต้องการ)
- กระเป๋า: เพิ่มแท็บ "ของดรอป", กล่องรายละเอียดแสดงประวัติ/เลเวล/ราคาขาย · **เลิกขายของจากกระเป๋า** → ต้องไปขายที่ร้าน
- ขายอุปกรณ์ที่พังได้ครึ่งราคา · หน้า "ร้านตีเหล็ก" แบบเดิมถูกแทนด้วยร้านค้าใหม่
- ผลการต่อสู้แสดงชื่อของดรอปถูกต้อง (`itemName()` ใน shared)
**ไฟล์หลักที่แตะ:** `packages/shared/src/data/items.ts`, `data/monsters.ts`, `data/shops.ts` (ใหม่), `rules/shop.ts` (ใหม่), `rules/shop.test.ts` (ใหม่),
`apps/game/src/ui/shopWindow.ts` (ใหม่), `ui/bagWindow.ts`, `ui/hud.ts`, `ui/homeWindows.ts`, `game/rules.ts`, `game/gear.ts`,
`scenes/HomeScene.ts`, `scenes/BattleScene.ts`, `pixel-theme.css`, `pixel-art/item-icons/build.py`, `tools/artpreview/avatar.mts`
**ทดสอบ:** npm test (90 ผ่าน) · typecheck ผ่าน · build ผ่าน · ในเบราว์เซอร์: วาร์ปไปห้าง → การ์ดมีปุ่ม "ร้านอุปกรณ์นักผจญภัย", ร้านของชำ:
ขายของดรอปทั้งหมด 127 → 209 G, ซื้อยา ×5 → 159 G, ปุ่มซื้อปิดเมื่อเงินไม่พอ, กระเป๋าแท็บของดรอปแสดงประวัติ+ราคาขาย,
สวมเกราะหวาย (Lv.12) ที่ Lv.4 ไม่ได้ / งอบใบลาน (Lv.2) ได้ · `npx tsx tools/artpreview/avatar.mts` เห็นชุดใหม่บนตัว
**ค้าง / ข้อควรรู้:** การซื้อขายยังคำนวณในเครื่องผู้เล่น (เซิร์ฟเวอร์ยังไม่ตรวจ) · ของดรอปยังใช้คราฟต์ไม่ได้ ·
เครื่องประดับไม่แสดงบนตัวละคร (ตั้งใจ) · เพิ่มไอเทมใหม่ = ใส่ใน `items.ts` (+ `level`, `lore`) + ไอคอนใน `pixel-art/item-icons/build.py` + ใส่ร้านใน `shops.ts`

## 2026-09-23 — เพลงต่อสู้/บอสใหม่ คุมธีมเดียวกับเพลงเมือง (Nong + Claude)
**แก้ไข / เปลี่ยน**
- เพลงต่อสู้แต่งใหม่ให้เป็น soundtrack ชุดเดียวกับเพลงแผนที่ (วอลทซ์) และเพลงบ้าน: ใช้วงเดียวกัน (โอโบ ฟลุต เครื่องสายดีด ฮาร์ป เซเลสตา แทมบูรีน)
  + เครื่องสายควบม้าและกลองมือให้ตื่นเต้น · E minor (คีย์พี่น้องของ G major) · 6/8 = วอลทซ์ที่เร่งเป็นการบุก ·
  ธีม A เปิดด้วยทำนองขึ้นแบบเดียวกับวอลทซ์ · โครง A1 → B → A2 (เข้มขึ้น) → ช่วงฮาร์ป+กลองรัว → B2
- เพลงบอสแต่งใหม่ด้วยวงเดียวกัน โทนมืดกว่า: E harmonic minor 6/8 + คณะประสานเสียง + เครื่องสายต่ำ + กลองหนัก
- เลิกใช้ทองเหลือง/เบสซินธ์ในเพลงต่อสู้ (ทองเหลืองเหลือใน fanfare ชนะ) · `drums()` รองรับกี่ step ต่อห้องก็ได้ (แทมบูรีนเป็น lane ได้)
- ปรับระดับเสียง: ต่อสู้/บอสดังกว่าเพลงแผนที่ ~1 dB (เดิม ~3 dB และ peak เกือบแตก)
**ไฟล์หลักที่แตะ:** `apps/game/src/game/music.ts`
**ทดสอบ:** npm test (77 ผ่าน) · typecheck ผ่าน · ตรวจทุกห้องของทำนองลงจังหวะครบ · เรนเดอร์ offline: ต่อสู้ RMS −20.5 / peak −3.3 dBFS,
บอส −20.2 / −4.3, แผนที่ −21.2, บ้าน −21.8 · ในเกม: แผนที่ → ต่อสู้ (battle) → กลับ (field) → บอส (boss) ไม่มี error
**ค้าง / ข้อควรรู้:** Claude ฟังเสียงเองไม่ได้ ต้องให้คนฟังแล้วติชม · ทำนองอยู่ที่ `BT_MEL_A/B`, `BS_MEL_A/B` (หน่วยจังหวะ = โน้ตเขบ็ต 1 ชั้น, 6 ต่อห้อง)

## 2026-09-23 — เข้าไปในบ้านได้ + เพลงวอลทซ์บนแผนที่ + fanfare ชนะ (Nong + Claude)
**เพิ่ม**
- **เข้าไปในบ้านได้** (`scenes/HomeScene.ts`): ห้องเรือนไม้ล้านนา pixel art (`pixel-art/home/build.py` — ผนังฝาปะกน, หน้าต่างเห็นดอยสุเทพ,
  โคมล้านนา, พรม, ผ้าขาวม้าบนเตียง) ตัวละครเดินไปหาเฟอร์นิเจอร์ที่แตะ (ขอบเรืองแสงสีทอง) แล้วเปิดหน้าต่างของจุดนั้น:
  เตียง = นอน (จอมืด Zzz → HP/MP เต็ม) · หีบ = ธนาคาร (ฝาก/ถอน 100 · 1,000 · ทั้งหมด) · โต๊ะช่าง = ซ่อม (แสดงความทนทาน+ค่าซ่อมรายชิ้น) ·
  ชั้นวาง = ร้านค้าแบบการ์ดไอคอน · กระจก = หน้าต่างตัวละคร · หม้อยา = เร็ว ๆ นี้ · ประตู/ปุ่ม "ออกจากบ้าน" = กลับแผนที่
- `ui/homeWindows.ts` หน้าต่างหีบ/โต๊ะช่าง/ชั้นวาง แบบ pixel
- **เพลงแผนที่ใหม่: วอลทซ์เมือง** แนว Theme of Prontera (RO) ตามที่ขอ — ใช้แค่เป็น ref แนวเพลง ทำนอง/คอร์ดแต่งใหม่ทั้งหมด:
  G major 3/4, โอโบ/ฟลุต + เครื่องสายดีด ตุ้ม-ชะ-ชะ + ฮาร์ป + เซเลสตา + แทมบูรีน · เครื่องดนตรีใหม่ `pizz`, `oboe`, `tambourine`
- **เสียงชนะใหม่**: fanfare orchestral ~3.5 วิ (ฮาร์ปไล่ขึ้น → แตรเรียก → คอร์ดใหญ่ + ไทโกะ/ฉาบ/เซเลสตา) ผ่าน reverb ของช่องเสียงเอฟเฟกต์
**แก้ไข / เปลี่ยน**
- เพลง ambient เดิมของแผนที่ย้ายไปเป็น **เพลงในบ้าน** (theme `home`)
- ปุ่ม "บ้าน": อยู่ในรัศมีบ้าน → เดินเข้าบ้าน, ยังไม่มีบ้าน/อยู่ไกล → หน้าตั้งบ้านเหมือนเดิม
- ระบบเพลงรองรับจังหวะ 3/4 (`Song.meter`)
- เสียงชนะเดิมมีจังหวะ/โน้ตคล้าย fanfare ของเกมดังเกินไป → เปลี่ยนเป็นของแต่งใหม่
**ไฟล์หลักที่แตะ:** `pixel-art/home/build.py` (ใหม่), `apps/game/public/assets/home/*` (ใหม่), `apps/game/src/scenes/HomeScene.ts` (ใหม่),
`apps/game/src/ui/homeWindows.ts` (ใหม่), `apps/game/src/game/music.ts`, `apps/game/src/game/audio.ts`, `apps/game/src/ui/hud.ts`,
`apps/game/src/scenes/WorldScene.ts`, `apps/game/src/game/bus.ts`, `apps/game/src/main.ts`, `apps/game/src/pixel-theme.css`
**ทดสอบ:** npm test (77 ผ่าน) · typecheck ผ่าน · build ผ่าน · ในเบราว์เซอร์ (เดสก์ท็อป + มือถือ 375×812): เข้าบ้าน → เพลงเปลี่ยนเป็นเพลงบ้าน,
หีบ ฝาก 100/ถอนทั้งหมดถูกต้อง, โต๊ะช่างแสดง 4 ช่อง, ร้านค้า, นอนแล้ว HP 10 → 211, ออกทางประตู → กลับแผนที่ + เพลงวอลทซ์,
ชนะการต่อสู้เล่น fanfare ไม่มี error · เรนเดอร์ offline: วอลทซ์/บ้าน/fanfare ไม่มีเสียงแตก ระดับเสียงใกล้เคียงกัน
**ค้าง / ข้อควรรู้:** หม้อปรุงยายังไม่ทำ (รอออกแบบสูตร) · Claude ฟังเสียงเองไม่ได้ ต้องให้คนฟังแล้วติชม ·
ขอบเรืองแสงตอนเอาเมาส์ชี้ใช้ไฟล์ `<id>_hi.png` ที่ build.py สร้างให้ — เพิ่มเฟอร์นิเจอร์ใหม่ให้ใส่ใน `FURNITURE` แล้วรันสคริปต์

## 2026-09-23 — เพลงใหม่แนว Orchestral / Ambient (Nong + Claude)
**เพิ่ม**
- `apps/game/src/game/music.ts` ระบบเรียบเรียงเพลง + เครื่องดนตรีสังเคราะห์: pad เครื่องสาย/คณะประสานเสียง, ฮาร์ป, เซเลสตา (FM),
  ฟลุต (มี vibrato + เสียงลม), เบสนุ่ม, เครื่องสายสับ (spiccato), ทองเหลือง, เบสขับ, ไทโกะ, สแนร์ (รัว), ไฮแฮต, ฉาบ + reverb ห้องใหญ่
- เพลงแผนที่ใหม่แนว VGM / New Age / Ambient (D major 76 bpm, 32 ห้อง 4 ท่อน ไม่มีกลอง) — ฟังตอนฟาร์มนาน ๆ ได้ไม่เหนื่อยหู
- เพลงต่อสู้ใหม่แนว orchestral เร้าใจ (A minor 152 bpm) และเพลงบอส (D harmonic minor 164 bpm + คณะประสานเสียง)
- `renderMusic()` (dev) เรนเดอร์เพลงแบบ offline เพื่อตรวจระดับเสียง/เสียงแตกโดยไม่ต้องใช้ลำโพง
**แก้ไข / เปลี่ยน**
- เปลี่ยนเพลงแบบ crossfade 0.9 วิ แทนการตัดทันที · AudioContext 32 kHz (ประหยัด CPU มือถือ)
- ตัดเพลงชิปทูนเดิมออก (เสียงเอฟเฟกต์ยังเป็นชิปทูนเหมือนเดิม)
**ไฟล์หลักที่แตะ:** `apps/game/src/game/music.ts` (ใหม่), `apps/game/src/game/audio.ts`
**ทดสอบ:** npm test (77 ผ่าน) · typecheck ผ่าน · เรนเดอร์ offline ทั้ง 3 เพลง: ไม่มีเสียงแตก (peak −4.6 ถึง −7.6 dBFS), ความดังใกล้กัน
(RMS −20 ถึง −21 dBFS) · ในเกม: เพลงแผนที่เล่นหลังแตะจอ → เข้าบอสเปลี่ยนเป็นเพลงบอส ไม่มี error
**ค้าง / ข้อควรรู้:** Claude ฟังเสียงเองไม่ได้ — ตรวจด้วยตัวเลข (ระดับเสียง/สเปกตรัม) ต้องให้คนฟังจริงแล้วติชม ·
ปรับทำนอง/คอร์ดได้ที่ `F_CHORDS`, `F_MEL1/2`, `B_MEL_A/B`, `X_MEL_*` ใน `music.ts` (เขียนเป็นชื่อโน้ต เช่น `'A4:1.5 D5:.5'`) ·
เพลงต่อสู้/บอสใช้ CPU มากกว่าเพลงแผนที่ ถ้ามือถือรุ่นเก่ากระตุกให้ลดไฮแฮต 16th หรือ reverb

## 2026-09-23 — เอฟเฟกต์สกิลในฉากต่อสู้ + เสียงเพลง/เสียงเอฟเฟกต์ (Nong + Claude)
**เพิ่ม**
- `pixel-art/fx/build.py` วาดเอฟเฟกต์ pixel 22 ชุด (48×48 ต่อเฟรม): ฟัน, ฟันเลือด, กระแทก, คริติคอล, ไฟ, น้ำ, สายฟ้า, น้ำแข็ง, ดิน,
  ศักดิ์สิทธิ์, เงา, พิษ, ฮีล, มานา, โล่, บัฟ, ดีบัฟ, มึน, วงเวท 3 แบบ, ควัน + กระสุน 7 แบบ (ลูกไฟ, ลูกแก้วน้ำ/แสง/เงา, ประกายไฟฟ้า, ลูกธนู, ก้อนหิน)
  → `apps/game/public/assets/fx/` ดูผลรวมที่ `pixel-art/fx/preview.png`
- `apps/game/src/scenes/battleFx.ts` เลือกเอฟเฟกต์ตามสกิล: สกิลเวท/สนับสนุน = วงเวทใต้เท้าผู้ร่าย, สกิลเวทหรือยิงไกล = กระสุนบินไปหาเป้า
  (ตามธาตุ) แล้วค่อยแสดงดาเมจ, ตีโดน = เอฟเฟกต์ตามธาตุ (ธาตุปกติ: ระยะประชิด = รอยฟัน / อื่น ๆ = กระแทก), คริ = ดาวแตก + จอสั่น,
  บล็อก = โล่, ติดสถานะ (มึน/แข็ง/พิษ/ไฟ/เลือด/ช้า/รัด/ยั่วยุ) มีเอฟเฟกต์ของตัวเอง, ฮีล/ยา/โล่/บัฟ/ดีบัฟ, ตาย = ควัน, บอสเปลี่ยนเฟส/คลั่ง
  + เป้าถูกกระแทกถอยเล็กน้อย
- `apps/game/src/game/audio.ts` เสียงชิปทูนสังเคราะห์สดด้วย Web Audio (ไม่มีไฟล์เสียง ไม่ติดลิขสิทธิ์):
  เสียงเอฟเฟกต์ 34 แบบ (ปุ่ม เปิด/ปิดหน้าต่าง เหรียญ เลเวลอัป เจอศัตรู วาร์ป แจ้งเตือน ฟัน คริ พลาด บล็อก ธาตุต่าง ๆ ฮีล ฯลฯ)
  + เพลงวน 3 เพลง: แผนที่ (สเกลเพนทาโทนิกแบบไทย 96 bpm), ต่อสู้ (148 bpm), บอส (164 bpm) + jingle ชนะ/แพ้
- ตั้งค่า → เปิด/ปิด และปรับระดับ **เพลง** / **เสียงเอฟเฟกต์** แยกกัน (จำไว้ใน localStorage `pw.audio`)
**แก้ไข / เปลี่ยน**
- เบราว์เซอร์อนุญาตเสียงหลังแตะจอครั้งแรกเท่านั้น → เพลงแผนที่เริ่มตอนแตะครั้งแรก
- เสียงประกอบ UI: ปุ่มทุกปุ่ม, เปิด/ปิดหน้าต่าง, ซื้อ/ขาย (เหรียญ), ใช้ยา, เลเวลอัป, ได้รับคำชวนปาร์ตี้, วาร์ป (โหมดจำลอง)
**ไฟล์หลักที่แตะ:** `pixel-art/fx/build.py` (ใหม่), `apps/game/public/assets/fx/*` (ใหม่), `apps/game/src/game/audio.ts` (ใหม่),
`apps/game/src/scenes/battleFx.ts` (ใหม่), `apps/game/src/scenes/BattleScene.ts`, `apps/game/src/main.ts`, `apps/game/src/ui/hud.ts`,
`apps/game/src/game/walk.ts`, `apps/game/src/pixel-theme.css`
**ทดสอบ:** npm test (77 ผ่าน) · typecheck ผ่าน · build ผ่าน · ทดสอบในเบราว์เซอร์: สนามทดสอบกับ กระสือ/กิ้งก่าไฟ/ปลาคาร์ป และบอสก็อบลิน
เห็นเอฟเฟกต์ครบ (slash, crit, fire, water, shadow, heal, shield, buff, วงเวท, กระสุน), เพลงเปลี่ยน แผนที่ → ต่อสู้/บอส → เงียบ+jingle → แผนที่,
ปุ่มเปิด/ปิดเพลงในตั้งค่าทำงาน
**ค้าง / ข้อควรรู้:** เสียงเป็นแบบสังเคราะห์ ปรับโทน/โน้ตได้ที่ `SONGS` และ `sfx()` ใน `audio.ts` · ยังไม่มีเสียงเดิน (ตั้งใจ — กันหนวกหู) ·
iPhone ต้องปิดโหมดเงียบ (สวิตช์ข้างเครื่อง) ถึงจะได้ยินเสียง Web Audio · สกิลใหม่ได้เอฟเฟกต์อัตโนมัติจากธาตุ/ประเภทดาเมจ ไม่ต้องแก้โค้ดเพิ่ม

## 2026-09-23 — หน้ากระเป๋าและปาร์ตี้ใหม่แบบ MMORPG (Nong + Claude)
**เพิ่ม**
- `apps/game/src/ui/bagWindow.ts` กระเป๋าแบบ MMORPG: แถบของที่สวมอยู่ 4 ช่อง, แท็บ ทั้งหมด / อุปกรณ์ / ของใช้ (มีจำนวน),
  ตารางช่องไอเทม (ไอคอน, จำนวนซ้อน, แถบความทนทาน, ขอบสีตามความหายาก), แตะไอเทม → กล่องรายละเอียด
  (ชื่อสีตามความหายาก, ช่อง, ค่าสถานะ, ความทนทาน, เอฟเฟกต์) พร้อมปุ่ม สวมใส่ / ถอด / ใช้ / ขาย (บอกราคาขาย) และเงินด้านล่าง
- `apps/game/src/ui/portrait.ts` รูปหน้าตัวละครจากชุดที่สวมจริง (ใช้ในการ์ดปาร์ตี้/รายชื่อผู้เล่น)
- หน้าปาร์ตี้ใหม่ (`ui/party.ts`): แท็บ **สมาชิก** = ปาร์ตี้ 3 ช่องเสมอ (การ์ดมีรูปหน้า ดาวหัวหน้า เลเวล อาชีพ ชิปสถานะ
  ออนไลน์/กำลังสู้/ระยะห่าง ปุ่มเชิญออก) + รายชื่อผู้เล่นใกล้ ๆ พร้อมปุ่มชวนหรือเหตุผลที่ชวนไม่ได้ ·
  แท็บ **ดันเจี้ยน** = การ์ดมีภาพบอสจริง ช่วงเลเวล จำนวนเวฟ ล็อกเป็นสีเทาถ้าเลเวลไม่ถึง
**แก้ไข / เปลี่ยน**
- แท็บของหน้าต่างย้ายลงมาเป็นแถบเต็มความกว้างใต้หัวหน้าต่าง (กดง่ายบนมือถือ)
**แก้บั๊ก**
- ชิปสถานะชื่อคลาส `near` ชนกับคลาส HUD เดิม → ใช้ `c-near` ฯลฯ
**ไฟล์หลักที่แตะ:** `apps/game/src/ui/bagWindow.ts` (ใหม่), `apps/game/src/ui/portrait.ts` (ใหม่), `apps/game/src/ui/party.ts`, `apps/game/src/ui/hud.ts`, `apps/game/src/pixel-theme.css`
**ทดสอบ:** npm test (77 ผ่าน) · typecheck ผ่าน · build ผ่าน · ทดสอบในเบราว์เซอร์จอมือถือ: ถอดอาวุธ → เห็นในตาราง → รายละเอียด → สวมกลับ, ใช้ยา,
ชวนผู้เล่นคนที่ 2 จากรายชื่อใกล้ ๆ → การ์ดปาร์ตี้ 2/3 + ปุ่มเชิญออก, แท็บดันเจี้ยน
**ค้าง / ข้อควรรู้:** HP ของสมาชิกปาร์ตี้ยังไม่แสดง (เซิร์ฟเวอร์ยังไม่ส่ง) · กระเป๋ายังไม่จำกัดจำนวนช่อง

## 2026-09-23 — มอนสเตอร์ 16-bit ใหม่ + สกิลมอน + บอสประจำถิ่นวัด/ห้าง + หมุดแลนด์มาร์ก (Nong + Claude)
**เพิ่ม**
- มอนสนามใหม่ 12 ชนิด (รวม 22 ชนิด Lv.1–32): นกพิราบลานวัด แมวดำหลังคา มดแดงทหาร หุ่นไล่กาขี้โมโห ตุ๊กแกยักษ์ หิ่งห้อยพราย
  สามล้อผีสิง ปูนาหุ้มเกราะ โคมลอยวิญญาณ งูเหลือมคูเมือง กระสือ ช้างหินโบราณ + มินเนียน "หุ่นโชว์เดินได้"
- สกิลเฉพาะตัวของมอน 25 สกิล (`data/skills.ts`) + `monsterSkillSlots` / `monsterDeck`: Lv.1–7 ใช้ 1 สกิล, 8–15 ใช้ 2, 16–23 ใช้ 3, 24+ ใช้ 4, บอสใช้ครบ
- บอสประจำถิ่นใหม่: **ห้างสรรพสินค้า** → ราชินีลดกระหน่ำกลางห้าง Lv.15 (เรียกหุ่นโชว์, แฟลชเซล) ·
  **วัด** → ยักษ์ทวารบาลเฝ้าประตู Lv.25 (กระบองสะเทือนธรณี) — ตรวจจับจากแผนที่ (`world.ts` poiKind)
- ภาพมอน/บอส 16-bit ความละเอียดจริง 30 ตัว (`pixel-art/monsters/build.py`) และหมุดแลนด์มาร์ก 6 แบบเป็นอาคาร pixel บนฐานสี
  (`pixel-art/landmarks/build.py`) วาดด้วยตัววาดของ skill pixel-art-studio · โหลดผ่าน `game/sprites.ts` (ภาพเก่าเป็น fallback)
- เทสต์ `monsters.test.ts` 5 ข้อ (สกิลมีจริง, จำนวนสกิลตามเลเวล, ทุกภูมิประเทศ/ช่วงเลเวลมีมอน, สมดุลบอสใหม่)
- `.claude/launch.json` เพิ่ม `game-http-5174` (พรีวิว HTTP ขณะ `dev:lan` รันอยู่)
**แก้ไข / เปลี่ยน**
- มอนเดิมได้สกิลเพิ่มตามเลเวล · ปรับสกิลแตรรถแดง/เสียงกรีดร้องค้างคาวให้เบาลงเพื่อรักษาสมดุลดันเจี้ยนเดิม
- มอน/หมุดแสดงด้วยขนาดพิกเซลเดียวกับตัวละคร (บนแผนที่ ×1.6, ในฉากต่อสู้เท่ากับฮีโร่)
- MASTER_SPEC: วัดมี "ยักษ์ทวารบาล" เป็นข้อยกเว้นเดียวของเขตปลอดภัย (ต้องกดท้าทายเอง ไม่มีปีศาจ/ผีในวัด)
**ไฟล์หลักที่แตะ:** `packages/shared/src/data/monsters.ts`, `packages/shared/src/data/skills.ts`, `packages/shared/src/types.ts`, `packages/shared/src/combat/engine.ts`, `apps/game/src/game/world.ts`, `apps/game/src/game/sprites.ts` (ใหม่), `apps/game/src/game/art.ts`, `apps/game/src/scenes/*.ts`, `pixel-art/monsters/build.py` (ใหม่), `pixel-art/landmarks/build.py` (ใหม่)
**ทดสอบ:** npm test (77 ผ่าน) · typecheck ผ่าน · build ผ่าน · ดูในเบราว์เซอร์: วัด/ห้าง/ร้านขึ้นบนแผนที่เชียงใหม่, มอนใหม่บนแผนที่และในฉากต่อสู้ขนาดพิกเซลตรงกับตัวละคร
**ค้าง / ข้อควรรู้:** สกิลมอนยังไม่มีไอคอน (ไม่แสดงใน UI) · มอนใหม่ต้องเพิ่มภาพใน `pixel-art/monsters/build.py` (ชื่อไฟล์ = `sprite`) ไม่งั้นใช้ภาพเก่าแทน ·
บอสเฉพาะเมือง (ประตูท่าแพ ดอยสุเทพ ฯลฯ) ยังเป็นแผน Phase 3

## 2026-09-23 — หน้าต่างสกิลแบบ RO + ไอคอนสกิล pixel (Nong + Claude)
**เพิ่ม**
- ไอคอนสกิล pixel 24×24 ครบ 35 ชิ้น (สกิล Novice 14 + 5 อาชีพ × 4 + โจมตีธรรมดา) วาดด้วยตัววาดของ skill pixel-art-studio
  (`pixel-art/skill-icons/build.py` → `apps/game/public/assets/skills/`) · สีกรอบ = ธาตุ · ตราสายฟ้าทอง = สกิลตอบโต้
- `apps/game/src/ui/skillWindow.ts` หน้าต่างสกิลแบบ RO: แท็บ `equipment | skill` บนแถบหัวหน้าต่างตัวละคร,
  ช่องชุดสกิล 6 ช่อง (3×2) แตะช่อง → เลือกสกิลจากรายการ (ไอคอน · % · CD · MP · ธาตุ · คำอธิบาย) / เอาออก / ชุดแนะนำ
- `packages/shared/src/rules/deck.ts` (`assignDeckSlot`, `clearDeckSlot`) — ใส่สกิลที่มีอยู่แล้วจะสลับช่อง ไม่ซ้ำ + เทสต์ 5 ข้อ
- ช่องสกิลในฉากต่อสู้แสดงไอคอน + ตัวเลขคูลดาวน์ทับบนไอคอน
**แก้ไข / เปลี่ยน**
- ลบตัวแก้ชุดสกิลแบบรายการเดิมในหน้าตัวละคร (แทนด้วยแท็บ skill)
**ไฟล์หลักที่แตะ:** `apps/game/src/ui/skillWindow.ts` (ใหม่), `pixel-art/skill-icons/build.py` (ใหม่), `packages/shared/src/rules/deck.ts` (ใหม่), `apps/game/src/ui/hud.ts`, `apps/game/src/ui/equipWindow.ts`, `apps/game/src/scenes/BattleScene.ts`, `apps/game/src/pixel-theme.css`
**ทดสอบ:** npm test (72 ผ่าน) · typecheck ผ่าน · build ผ่าน · ทดสอบในเบราว์เซอร์จอมือถือ: สลับแท็บ, ใส่ปาหินลงช่อง 1, เอาออกช่อง 3, ฉากต่อสู้เห็นไอคอน + CD
**ค้าง / ข้อควรรู้:** สกิลใหม่ต้องเพิ่มไอคอนใน `pixel-art/skill-icons/build.py` แล้วรัน `python pixel-art/skill-icons/build.py` · สกิลมอนสเตอร์ยังไม่มีไอคอน (ไม่ได้แสดงใน UI)

## 2026-09-23 — UI ทั้งเกมเป็น pixel 16-bit + ช่องสวมใส่เหลือ 4 ช่อง (Nong + Claude)
**เพิ่ม**
- ชุด UI 16-bit วาดด้วย skill pixel-art-studio (`pixel-art/ui-kit/build.py` → `apps/game/public/assets/ui/`):
  กรอบหน้าต่าง, ปุ่ม 5 สี (ปกติ/ส้ม/น้ำเงิน/แดง/เข้ม) ทั้งแบบนูนและแบบกด, ช่องบุ๋ม, กรอบเลือก + ไอคอน 16×16 จำนวน 19 ชิ้น
  (ตัวละคร กระเป๋า ปาร์ตี้ บ้าน ตั้งค่า AUTO ซูม เข็มทิศ วิ่ง หมุด เหรียญ ดาบไขว้ หัวกะโหลก ปิด หัวใจ หยดน้ำ ดาว แชท)
- `apps/game/src/pixel-theme.css` ธีมพิกเซลทับทุกหน้าจอ (ยกเว้นแผนที่): HUD, แถบ HP/MP/EXP, เมนูล่าง, การ์ด, toast, หน้าต่าง,
  ป๊อปอัป, หน้าสร้างตัวละคร, ปาร์ตี้, ฉากต่อสู้ · ฟอนต์ Silkscreen (อังกฤษ/ตัวเลข) + Chakra Petch (ไทย) รวมถึงตัวหนังสือใน Phaser
- ไอเทมเครื่องประดับใหม่: สร้อยข้อมือเชือกนำโชค (LUK+1, ของเริ่มต้น), เครื่องรางนักวิ่ง (AGI+8), จี้อีจีส (DEF+40 VIT+6) + ไอคอน
- `migrateLegacyGear` แปลงเซฟเก่าอัตโนมัติ + เทสต์ 4 ข้อ
**แก้ไข / เปลี่ยน**
- **ช่องสวมใส่เหลือ 4 ช่อง: ชุด / หัว / เครื่องประดับ / อาวุธ** — ตัดรองเท้าและมือรอง; รองเท้ามากับชุด (วาดใน `gear.ts`)
- ตัดของ: รองเท้าวิ่ง/โล่อีจีส (แปลงเป็นเครื่องประดับ), รองเท้าแตะสาน (คืน 50 G) · ตัดโบนัสบล็อกจากโล่ · ภูตใบไม้ดรอปเครื่องรางนักวิ่งแทน
- ช่องว่างในหน้าต่างสวมใส่เป็นช่องบุ๋มมีเครื่องหมาย "+" แทนไอคอนเงาจาง (เดิมดูเหมือนรูปโหลดไม่ขึ้น)
- อีโมจิบนหน้าจอหลัก/หัวข้อ/ปุ่ม เปลี่ยนเป็นไอคอน pixel · ตัวเลข HP/SP ย่อให้ไม่ล้นบรรทัด
**แก้บั๊ก**
- ลบ `apps/game/src/game/icons.ts` (ไอคอนเก่าที่ไม่มีใครใช้)
**ไฟล์หลักที่แตะ:** `apps/game/src/pixel-theme.css` (ใหม่), `apps/game/src/ui/pixel.ts` (ใหม่), `pixel-art/ui-kit/build.py` (ใหม่), `packages/shared/src/types.ts`, `packages/shared/src/data/items.ts`, `packages/shared/src/rules/starter.ts`, `apps/game/src/game/gear.ts`, `apps/game/src/ui/*.ts`, `apps/game/src/scenes/*.ts`, `apps/game/index.html`
**ทดสอบ:** npm test (67 ผ่าน) · typecheck ผ่าน · build ผ่าน · ดูในเบราว์เซอร์จอมือถือ 375 px + จอคอม: หน้าสร้างตัวละคร, HUD บนแผนที่, หน้าตัวละคร, กระเป๋า, ปาร์ตี้, ฉากต่อสู้ + ป๊อปอัปชนะ — ไม่มี error
**ค้าง / ข้อควรรู้:** ภาษาไทยยังเป็นฟอนต์ Chakra Petch (ไม่ใช่พิกเซลแท้ — ยังไม่มีฟอนต์พิกเซลไทยฟรี) · ไอคอนธาตุสกิล 🔥💧⚡ และไอคอนดันเจี้ยนยังเป็นอีโมจิ ·
เพิ่มปุ่ม/หน้าต่างใหม่ให้ใช้คลาสเดิม (`.btn`, `.mini`, `.pick`, `.sheet` …) จะได้ธีมพิกเซลอัตโนมัติ

## 2026-09-23 — หน้าต่างสวมใส่สไตล์ RO + ไอคอนไอเทม pixel art (Nong + Claude)
**เพิ่ม**
- `apps/game/src/ui/equipWindow.ts` หน้าต่าง "ไอเทมที่สวมใส่" แบบ RO classic: ช่องซ้าย หัว/อาวุธ/เครื่องประดับ · ขวา ชุด/มือรอง/รองเท้า ·
  ตัวละครเดินหมุนโชว์ตรงกลาง · แถบเลือกของของช่องที่แตะ · "▲ สถานะ" (Str–Luk ฐาน + โบนัสจากของ, Atk/Matk/Def/Mdef/Critical/Flee/Speed/HP·SP)
  - **หน้าสร้างตัวละคร**: แตะช่อง → เลือกของเริ่มต้น (ไม่ใส่ / ตัวเลือก + ราคา) · ค่าสถานะ Lv.1 และทองที่เหลืออัปเดตสด
  - **ในเกม** (👤 ตัวละคร): แตะช่อง → ถอด หรือสวมของในกระเป๋าที่ใช้ช่องนั้น · ปุ่ม + อัป Stat · ของพังแสดงเป็นสีเทา
- ไอคอน pixel art 24×24 (วาดด้วย skill pixel-art-studio, แสงซ้ายบน, สีไล่เฉดแบบ hue-shift, เส้นขอบหมึกเดียวกันทั้งชุด) 17 ชิ้น:
  อุปกรณ์ทุกชิ้น + ยา HP/MP, หินลับ, กล่องซ่อม · ไอคอนจาง ๆ ของช่องว่าง 6 ช่องแบบ RO
  ต้นฉบับ `pixel-art/item-icons/build.py` → ส่งออก `apps/game/public/assets/items/*.png`
- `ui/heroPreview.ts` ตัวพรีวิวตัวละครเดิน/หมุน ใช้ร่วมกันทุกหน้า
**แก้ไข / เปลี่ยน**
- หน้าตัวละครเดิม (แถว Stat +1/+5 และตารางค่าพลัง) ถูกแทนด้วยหน้าต่าง RO · กระเป๋าแสดงไอคอนของที่สวมอยู่ + ปุ่มไปหน้าต่างสวมใส่
- รายการของในกระเป๋า/ร้านค้าแสดงไอคอน · ลบ CSS/โค้ดหน้าสร้างตัวละครแบบเดิมที่ไม่ใช้แล้ว
**ไฟล์หลักที่แตะ:** `apps/game/src/ui/equipWindow.ts` (ใหม่), `apps/game/src/ui/heroPreview.ts` (ใหม่), `apps/game/src/ui/creator.ts`, `apps/game/src/ui/hud.ts`, `apps/game/src/style.css`, `pixel-art/item-icons/build.py` (ใหม่), `apps/game/public/assets/items/` (ใหม่)
**ทดสอบ:** npm test (63 ผ่าน) · typecheck ผ่าน · ทดสอบในเบราว์เซอร์ (จอคอม + มือถือ 375 px): เลือก/ยกเลิกของในหน้าสร้าง (ทองถูกต้อง), สร้างตัวละคร, แตะช่องอาวุธ → ถอด → สวมกลับจากกระเป๋า, กระเป๋าแสดงไอคอน, ไม่มี error ใน console
**ค้าง / ข้อควรรู้:** ไอเทมใหม่ต้องเพิ่มไอคอนใน `pixel-art/item-icons/build.py` แล้วรัน `python pixel-art/item-icons/build.py` (ต้องมี Pillow) ไม่งั้นช่องจะเป็นภาพแตก ·
ไอคอนแสดงที่ 2 เท่า (48 px) เสมอเพื่อให้พิกเซลคม — บนจอแคบชื่อของจะย้ายไปอยู่ใต้ไอคอน

## 2026-09-23 — ติดตั้ง skill วาด pixel art (pixel-art-studio) ให้ทั้งทีม (Nong + Claude)
**เพิ่ม**
- `.claude/skills/pixel-art-studio/` จาก https://github.com/Gamezxz/pixel-art-studio (MIT, commit `f8c2466`) —
  Claude วาด pixel art ด้วยสคริปต์ Python + Pillow แล้วเปิดดู/ตรวจ/แก้วนซ้ำ ส่งออก PNG / GIF / spritesheet+JSON
  (ตัดภาพตัวอย่างใน `docs/` และ `pixel-art/` ของต้นฉบับออก เหลือ 285 KB · ที่มาอยู่ใน `UPSTREAM.txt`)
- กฎงานภาพใน `CLAUDE.md` (หัวข้อ Pixel art): งานภาพนิ่งใหม่ใช้ skill นี้ เก็บ `pixel-art/<slug>/build.py` ไว้ใน git เป็นต้นฉบับ
- README: วิธีติดตั้ง Pillow สำหรับคนทำงานภาพ
**ทดสอบ:** ตรวจสคริปต์ทุกไฟล์แล้ว ไม่มีการต่อเน็ต/รันคำสั่งระบบ · ติดตั้ง Pillow 12.3 · รันตัวอย่างสไลม์ 4 เฟรมได้ PNG/GIF/spritesheet ครบ
**ค้าง / ข้อควรรู้:** เครื่องที่จะใช้ skill ต้อง `python -m pip install --user pillow` ก่อน · skill ช่วยงานภาพนิ่ง (ไอคอน มอนสเตอร์ ตัวละครของแท้)
ส่วนอุปกรณ์บนตัวละครยังวาดสดด้วย `gear.ts` เหมือนเดิม

## 2026-09-23 — เลือกอุปกรณ์ในหน้าสร้างตัวละคร + ระบบสวมใส่แสดงบนตัวละคร (Nong + Claude)
**เพิ่ม**
- หน้าสร้างตัวละครมีส่วน "🎒 อุปกรณ์เริ่มต้น": งบ 500 Gold เลือกใส่ทีละช่อง (หัว/ตัว/อาวุธ/เท้า/ยา) ชิ้นที่ไม่ใส่เก็บเป็นทอง
  มีปุ่มลัด "ใส่ครบชุด" / "ตัวเปล่า" และพรีวิวตัวละครใส่ของจริงแบบสด — **แทนหน้าเลือก A/B (Naked Dilemma) เดิม**
- ระบบวาดอุปกรณ์บนตัวละคร 48×64 (`apps/game/src/game/gear.ts`): เสื้อผ้าฝ้าย (ย้อมตาม "สีเสื้อ"), เกราะหัวจ่ายน้ำมัน,
  หมวกเหล็ก, ผ้าโพกหัว, รองเท้าวิ่ง, รองเท้าแตะ, ดาบไม้, ดาบพิกเซล, กระบองโบโลน่า, ไม้เท้าฝึกหัด, คทาละอองดาว, โล่อีจีส, ชุดชั้นใน
  - วาดจากรูปร่างหุ่นของแต่ละเฟรม (เส้นขอบ/เงา/สีพื้น) → ชุดขยับตามทุกท่าเดิน/ยืนเอง ไม่ต้องวาดแยกทีละเฟรม
  - อาวุธถืออยู่ที่มือที่หาเจอในแต่ละเฟรม · ไม้เท้าอยู่หลังตัว หัวคทาอยู่หน้าสุด
- ไอเทมใหม่ 3 ชิ้น: ผ้าโพกหัว (DEF+1), รองเท้าแตะสาน (AGI+1), ไม้เท้าฝึกหัด (MATK+5) — ขายที่ร้านบ้านด้วย
- `packages/shared/src/rules/starter.ts` (คิดทองเริ่มต้น + กันเลือกของนอกรายการ) + เทสต์ 5 ข้อ
- เครื่องมือ `npx tsx tools/artpreview/avatar.mts` เรนเดอร์ทุกชุด × ทุกท่าเป็นภาพ PNG (ไม่ต้องติดตั้งอะไรเพิ่ม)
**แก้ไข / เปลี่ยน**
- ราคาเสื้อผ้าฝ้าย/ดาบไม้ 20 → 150 Gold ให้เท่ากับราคาในหน้าสร้างตัวละคร (ใส่ครบ = เหลือ 50 G, ตัวเปล่า = 500 G เท่าเดิม)
- ปุ่ม "สีชุด" เปลี่ยนเป็น "สีเสื้อ" และจะกดได้เฉพาะตอนใส่เสื้อผ้าฝ้าย
- `Paperdoll` / `PlayerLook` มีช่อง `offhand` (โล่แสดงบนตัว และผู้เล่นอื่นเห็นด้วย)
- `SaveData.starter` เพิ่มค่า `'CUSTOM'` (เซฟเก่า STANDARD/NAKED ยังใช้ได้)
**ไฟล์หลักที่แตะ:** `apps/game/src/game/gear.ts` (ใหม่), `apps/game/src/game/avatar.ts`, `apps/game/src/ui/creator.ts`, `apps/game/src/ui/hud.ts`, `apps/game/src/state/store.ts`, `packages/shared/src/data/items.ts`, `packages/shared/src/rules/starter.ts`, `tools/artpreview/avatar.mts`, `tools/artpreview/png.ts`
**ทดสอบ:** npm test (63 ผ่าน) · typecheck ผ่าน · build ผ่าน · ทดสอบในเบราว์เซอร์: สร้างตัวละคร (เพศหญิง+เสื้อ+ไม้เท้า+ยา → เหลือ 150 G ถูกต้อง), เห็นชุดบนแผนที่และฉากต่อสู้, ถอดของในกระเป๋าแล้วตัวละครเปลี่ยนทันที, หน้าจอมือถือ 375 px
**ค้าง / ข้อควรรู้:** แหวนไม่แสดงบนตัว (ตั้งใจ) · ไอเทมใหม่ต้องมี `sprite` ที่ `gear.ts` รู้จัก ไม่งั้นจะไม่เห็นบนตัว · ภาพหุ่นยังเป็น Prototype (Sword of Convallaria) — ระบบอุปกรณ์วาดจากรูปร่างหุ่น จึงใช้ต่อได้เมื่อเปลี่ยนเป็นภาพของเราเองที่ layout เดิม

## 2026-09-23 — อัปเดต Roadmap กราฟิก (32-bit HD-2D Transition) และสรุปสถานะ Avatar Pack บน main (Claude)
**เพิ่ม**
- `docs/ROADMAP.md`: เพิ่มรายละเอียดแผนการพัฒนางานภาพสู่ 32-bit HD-2D:
  - วาด Asset อวาตาร์ของแท้ (Original Art 48×64) เพื่อทดแทน Prototype Asset ก่อนเปิด Public
  - แผนจัดทำ Dedicated Monster Pack ความละเอียดจริง (32–48 px, บอส 64 px) เพื่อแก้ปัญหาสไปรต์ยืดพิกเซล (Mixels)
  - แผนพัฒนาระบบเลเยอร์เสื้อผ้า/อุปกรณ์ (Equipment/Outfit Layering) บนหุ่น 48×64
  - เพิ่มหัวข้อรอตัดสินใจ: ทิศทางความละเอียดสไปรต์ (16-bit vs 32-bit HD-2D) และระบบชุด (Modular vs Full Outfit)
  - บันทึก Checklist และข้อจำกัดลิขสิทธิ์ของ Prototype Asset ใน Known Limitations
**แก้ไข / เปลี่ยน**
- รวม `feature/avatar-pack` เข้าสู่ branch `main` และ push ขึ้น GitHub (`origin/main`) สำเร็จ
- อัปเดตสถานะใน `CHANGELOG.md` และ `docs/ROADMAP.md` ให้สอดคล้องกับโค้ดล่าสุดบน `main`
**ไฟล์หลักที่แตะ:** `docs/ROADMAP.md`, `CHANGELOG.md`
**ทดสอบ:** npm test (58 ผ่าน) · typecheck ผ่าน
**ค้าง / ข้อควรรู้:** Asset ตัวละคร 48×64 ปัจจุบันเป็น Prototype Placeholder จาก Sword of Convallaria สำหรับทดสอบภายในเท่านั้น ห้ามเผยแพร่เชิงพาณิชย์

## 2026-09-23 — นำเข้า Avatar Pack (Base Body + Hairstyles) แทนตัวละครเดิม (Claude)
**เพิ่ม**
- Avatar Pack: คัดลอก assets (body, hair, manifest) ทั้งหมด 115 ไฟล์ไปที่ `apps/game/public/assets/avatar/`
- `apps/game/src/game/avatar.ts`: ตัวโหลด manifest แบบ dynamic, ตัวแปลงสี (recolor) ผิว/ผมตาม tone encoding ใน HANDOFF.md, ประกอบเลเยอร์ body + hair ใน cell 48×64 (anchor 24, 58) และระบบ caching
- เพิ่ม gender ('male' | 'female') ใน `Appearance` (`art.ts`, `packages/shared/src/net/protocol.ts`, `state/store.ts`) พร้อมระบบ migration เซฟเดิมไม่ให้พัง
- หน้าสร้างตัวละคร (`ui/creator.ts`): เพิ่มปุ่มเลือกเพศ ชาย/หญิง, วนทรงผมเฉพาะเพศที่เลือกพร้อมชื่อไทยจาก manifest, และ live preview หมุน 4 ทิศทาง
- Feature flag `USE_AVATAR_PACK` ควบคุมการเปิดใช้ โดยเก็บระบบ procedural 16×24 เดิมเป็น fallback
**แก้ไข / เปลี่ยน**
- `WorldScene.ts`: ปรับสเกลสไปรต์ตัวละคร (1.6), สเกลมอนสเตอร์ (~32–48 px, บอส ~56 px), จุด origin (0.5, 58/64), แอนิเมชันเดิน 4 เฟรม, และการกลับด้านซ้ายขวาตามทิศทาง
- `remotePlayers.ts`: รองรับ origin และสเกลใหม่สำหรับผู้เล่นคนอื่นในโหมด avatar pack
- `BattleScene.ts`: ปรับจุดยืนและสเกลของตัวละครผู้เล่น (×0.55) และมอนสเตอร์ (~32–48 px, บอส ~64 px) ในสนามประลองให้ตรงกับ baseline ของกันและกัน
**ไฟล์หลักที่แตะ:** `apps/game/src/game/avatar.ts`, `apps/game/src/game/art.ts`, `apps/game/src/ui/creator.ts`, `apps/game/src/scenes/WorldScene.ts`, `apps/game/src/scenes/remotePlayers.ts`, `apps/game/src/scenes/BattleScene.ts`, `apps/game/src/state/store.ts`, `packages/shared/src/net/protocol.ts`, `apps/game/src/style.css`
**ทดสอบ:** npm test (58 ผ่าน) · typecheck ผ่าน · build ผ่าน (Vite) · ตรวจสอบไฟล์อ้างอิงใน manifest ทั้งหมด 115 ไฟล์ครบถ้วน
**ค้าง / ข้อควรรู้:** avatar pack ชุดนี้เป็นภาพ prototype placeholder ยังไม่มีชุดเสื้อผ้าและอุปกรณ์สวมใส่ รวมเข้า `main` แล้ว

## 2026-09-22 — ตั้งกฎทีม + เอกสารส่งต่อ + ขึ้น GitHub (Nong + Claude)
**เพิ่ม**
- `CHANGELOG.md` (ไฟล์นี้), `docs/ROADMAP.md` (สถานะ + งานถัดไป + เรื่องที่ต้องตัดสินใจ)
- กฎการทำงานของทีมใน `CLAUDE.md` — ทุก session ต้องอ่านก่อน/บันทึกหลังทำ
- `README.md` เขียนใหม่เป็นคู่มือติดตั้งและรันทุกโหมด + แก้ปัญหาที่เจอบ่อย
**ไฟล์หลักที่แตะ:** `CLAUDE.md`, `README.md`, `CHANGELOG.md`, `docs/ROADMAP.md`, `.gitignore`
**ทดสอบ:** npm test (58 ผ่าน) · typecheck ผ่าน
**ค้าง / ข้อควรรู้:** push ขึ้น `github.com/bosocmputer/game_pixcel_walker` branch `main`

## 2026-09-22 — ปาร์ตี้สู้มอนด้วยกัน + สายจูง 200 ม. + ยอมรับก่อนลงดัน
**เพิ่ม**
- ตีมอนบนแผนที่: สมาชิกปาร์ตี้ที่อยู่ใกล้ ≤ 200 ม. และว่าง ถูกดึงเข้าสู้ด้วยอัตโนมัติ (HP มอน × n^0.6, EXP เต็มทุกคน)
- สายจูงปาร์ตี้: ห่างจากสมาชิกทุกคนเกิน 200 ม. → เตือน → หลุดปาร์ตี้หลัง 10 วิ (`rules/party.ts`)
- ดันเจี้ยนต้องกดยอมรับทุกคนภายใน 20 วิ มีคนปฏิเสธ/ไม่ตอบ = ยกเลิก
**แก้ไข / เปลี่ยน**
- โปรโตคอลเปลี่ยนจาก `dungeon:*` เป็น `run:*` (open/prepare/status/cancel/begin) ใช้ร่วมกันทั้งดันเจี้ยนและสู้มอน
**แก้บั๊ก**
- สถานะ busy ของ client ตั้งตอนการต่อสู้เริ่มจริง (`battle:launched`) ไม่ใช่ตอนขอเริ่ม
**ไฟล์หลักที่แตะ:** `apps/server/src/index.ts`, `apps/game/src/game/net.ts`, `apps/game/src/ui/party.ts`, `packages/shared/src/rules/party.ts`, `packages/shared/src/net/protocol.ts`
**ทดสอบ:** npm test (58 ผ่าน) · ทดสอบ 2 แท็บ: ปฏิเสธ/ยอมรับดัน, สู้มอนด้วยกัน, วาร์ปออกนอกระยะแล้วหลุด

## 2026-09-22 — ระบบปาร์ตี้ + ดันเจี้ยน instance + เครื่องมือเดินจำลอง
**เพิ่ม**
- ปาร์ตี้สูงสุด 3 คน (ชวน/รับ/เชิญออก/ออก) ผ่าน presence server
- ดันเจี้ยน 3 แห่ง (`data/dungeons.ts`): ตรอกหมาผี, โกดังพญาก็อบลิน, ปั๊มร้างยามค่ำ — ทุกเครื่องจำลองการต่อสู้เองจาก seed เดียวกัน
- ยาของใครของมัน (`UnitSetup.items`), ของดรอปส่วนตัว (`memberLootSeed`)
- โหมดจำลอง: ความเร็ว 1×–300×, คลิกขวา/กดค้างเพื่อวาร์ป, ปุ่ม 📍 กลับคูเมือง
**แก้ไข / เปลี่ยน**
- สเกล HP บอสตามจำนวนคนแบบ n^0.6 (ไปหลายคนได้เปรียบ)

## 2026-09-22 — ประวัติก่อนมีไฟล์นี้ (สรุปจาก git log)
- วางรากฐาน: MASTER_SPEC, กฎเกม/ข้อมูลใน `packages/shared`
- แผนที่จริง (MapLibre + OpenFreeMap) + สไปรต์ 16-bit, ตัวสร้างตัวละคร, เดิน 4 ทิศ
- มอนสเตอร์บนแผนที่ (EXP จากการตีมอนเท่านั้น), รัศมีสู้ 120 ม., มอนหนาแน่น, Auto Hunt
- ตัดระบบ Walk Session/นับก้าวออก, บล็อกการสู้เมื่อเร็วระดับรถ
- กล้องหมุน/เอียง/ซูมอิสระ
- ระบบต่อสู้ใหม่ Party Auto Turn-Based (แบบ Pockie Ninja) แทน ATB — `docs/COMBAT_SPEC.md`
- Skill Deck Novice 14 สกิล + ชุดสำเร็จรูป 5 แบบ, สนามทดสอบ + หุ่นไม้, MP ใช้จริง
- LAN multiplayer: เห็นผู้เล่นอื่นบนแผนที่ (presence server)
